# 业务模型语义执行闭环开发计划

> 编码要求：本文档及后续相关代码、配置、测试文件统一采用 UTF-8 编码。
>
> 兼容性要求：业务模型语义闭环优化不得丢失已有功能，不得影响普通查询、预测、预警、警报、建模、看板钉入、复合任务等既有链路；所有改造必须支持无业务模型场景平滑回退。

## 一、建设目标

当前业务模型、业务字典、指标公式已经能够被保存和维护，但在普通对话查询链路中还没有形成稳定闭环。现阶段业务模型更多是作为知识图谱或 AI 提示参与 SQL 生成，无法保证用户配置的字段、维度、公式一定被最终 SQL 使用。

本计划目标是将业务模型从“参考信息”升级为“正式语义约束”，让用户在业务模型中配置的字段、维度、字典、公式，能够在普通对话查询、预测、预警、复合任务中稳定生效。

完整闭环如下：

```text
用户配置业务模型
-> 对话请求携带当前业务模型上下文
-> 后端加载业务模型语义上下文
-> 解析用户语义命中业务项
-> 按业务模型配置生成字段、维度、公式执行计划
-> 生成 SQL 或分析任务参数
-> 执行前校验是否真正使用业务模型配置
-> 返回结果和业务语义命中说明
-> 业务模型更新后缓存自动失效
```

## 二、现状判断

### 1. 已经具备的能力

- 业务模型可以保存 `dictionaryEntries`、`metricDefinitions`、`dimensionSystem`。
- 业务模型更新后会触发知识图谱同步。
- 普通查询会调用知识图谱生成 `graphSqlHints`。
- 知识图谱会读取同表 `ACTIVE` 业务模型中的业务字典和指标公式，作为候选信息传给 AI。
- 前端对话请求已经能够传递 `activeBusinessModelId`、`lastCreatedBusinessModelId`、`lastAppliedBusinessModelId` 等上下文。

### 2. 当前不足

- 普通查询主链路没有严格按 `activeBusinessModelId` 加载当前选中的业务模型。
- 业务字典主要作为字段候选增强，不是强制字段约束。
- 指标公式目前更像提示，没有稳定编译为 SQL 表达式。
- Java 兜底规则仍主要依赖内置同义词和字段类型，不读取用户业务模型配置。
- 语义缓存未纳入业务模型版本，可能导致模型修改后继续复用旧 SQL。
- 查询结果缺少明确的业务模型命中 trace，难以判断配置是否真的生效。

## 三、核心设计

### 1. 新增 BusinessSemanticContext

新增业务语义上下文对象，用于统一承载当前对话可用的业务模型配置。

建议包含：

- `modelId`：当前业务模型 ID。
- `modelName`：当前业务模型名称。
- `tableName`：绑定数据表。
- `modelVersion` 或 `updatedAt`：用于缓存失效。
- `source`：模型来源，例如 `activeBusinessModel`、`lastAppliedModel`、`tableDefaultModel`。
- `dictionaryEntries`：业务字典、同义词、业务项到字段绑定。
- `metricDefinitions`：指标字段、聚合方式、公式、口径说明。
- `dimensionSystem`：业务维度和物理字段绑定。
- `ambiguities`：歧义项，例如一个业务词对应多个字段。

### 2. 新增 BusinessSemanticResolver

新增业务语义解析器，将用户自然语言解析为结构化业务执行计划。

示例：

```json
{
  "metrics": [
    {
      "name": "收入",
      "field": "sales_amt",
      "formula": "sales_amt",
      "aggregation": "SUM",
      "source": "metricDefinitions"
    }
  ],
  "dimensions": [
    {
      "name": "省份",
      "field": "province",
      "source": "dimensionSystem"
    }
  ],
  "time": {
    "granularity": "month",
    "range": "this_year"
  },
  "semanticTrace": {
    "matchedTerms": ["收入", "省份"],
    "modelId": 12,
    "source": "activeBusinessModel"
  }
}
```

解析能力要求：

- 支持业务字典命中。
- 支持同义词命中。
- 支持指标命中。
- 支持维度命中。
- 支持公式指标命中。
- 支持歧义检测。
- 支持未命中时回退原有链路。

### 3. 明确业务模型优先级

字段、维度、公式选择优先级建议为：

1. 用户明确提到的业务模型项。
2. 当前选中业务模型中的指标、维度、字典绑定。
3. 知识图谱候选。
4. Python AI 生成结果。
5. Java fallback 规则。

示例：

如果用户问“看收入走势”，当前业务模型中配置“收入 = sales_amt”，则 SQL 必须优先使用 `sales_amt`，不能让 AI 自由选择其他疑似收入字段。

### 4. 新增公式编译器

将业务模型中的指标公式编译为安全 SQL 表达式。

示例配置：

```text
毛利率 = profit / sales_amt
```

用户输入：

```text
看一下各省毛利率排名
```

期望 SQL 表达式：

```sql
SUM(CAST(NULLIF(`profit`, '') AS DECIMAL(18,2)))
/ NULLIF(SUM(CAST(NULLIF(`sales_amt`, '') AS DECIMAL(18,2))), 0)
```

安全要求：

- 只允许业务模型已绑定字段。
- 只允许数字、字段名、四则运算、括号、白名单聚合函数。
- 禁止 SQL 关键危险操作。
- 除法必须自动增加 `NULLIF(..., 0)` 保护。
- 字段不存在或字段类型不匹配时不得执行，应提示确认。

### 5. SQL 生成后强校验

生成 SQL 后增加业务模型一致性校验。

校验内容：

- 用户命中的指标是否真的出现在 SQL 中。
- 用户命中的维度是否真的用于 `SELECT` 或 `GROUP BY`。
- 公式指标是否真的使用了公式表达式。
- 字典绑定字段是否被正确使用。
- 时间字段、筛选字段是否与模型或字段元数据一致。

处理策略：

- 能自动修正时，重建 SQL。
- 无法修正但存在明确错误时，拒绝执行并提示原因。
- 语义不明确时，进入确认流程。

### 6. 缓存改造

语义缓存 key 必须纳入业务模型上下文。

建议缓存 key 包含：

- `question`
- `tableName`
- `activeBusinessModelId`
- `businessModelVersion` 或 `businessModelUpdatedAt`

业务模型更新后，应使相关缓存自动失效，避免用户修改配置后继续命中旧 SQL。

### 7. 返回业务语义 trace

每次查询结果中增加 `businessSemanticTrace`，用于前端展示、调试和验收。

示例：

```json
{
  "modelId": 12,
  "modelName": "销售业务模型",
  "source": "activeBusinessModel",
  "matchedMetric": "收入",
  "resolvedMetricField": "sales_amt",
  "matchedDimension": "省份",
  "resolvedDimensionField": "province",
  "formulaApplied": false,
  "dictionaryMatched": true,
  "dictionaryHitTerm": "GMV",
  "finalSqlValidated": true
}
```

## 四、分阶段实施计划

### 阶段分类说明

- 基础开发：P0 至 P4。目标是完成普通对话查询中的业务模型语义闭环，让字段、维度、字典、公式能够真实影响 SQL 生成和执行校验。
- 进阶开发：P5 至 P6。目标是将业务模型语义能力扩展到预测、预警、复合任务，并补齐缓存、审计、可观测性等生产增强能力。

### P0：回归基线和保护（基础开发）

目标：先建立测试保护，避免后续优化影响已有功能。

开发内容：

- 梳理普通查询、TopN、趋势、对比、预测、预警、钉入看板、复合任务的基础回归用例。
- 增加无业务模型场景测试。
- 增加有业务模型但未命中业务词的回退测试。

验收标准：

- 没有选择业务模型时，原有普通查询链路不受影响。
- 没有命中业务模型语义时，仍能走原有 AI 和 Java fallback。
- 预测、预警、钉入看板、复合任务不出现行为回退。

### P1：接入当前业务模型上下文（基础开发）

目标：普通对话查询能够明确加载当前选中的业务模型，而不是只读取同表所有模型。

开发内容：

- 在 `ChatBiService` 中读取 `activeBusinessModelId`。
- 新增业务模型上下文加载逻辑。
- 明确模型来源优先级：当前选中模型 > 最近应用模型 > 最近创建模型 > 同表默认模型。
- 将业务模型上下文传入 SQL 生成、校验和审计链路。

验收标准：

- 查询响应中包含 `businessSemanticTrace.modelId`。
- 当前选中的模型优先于同表其他模型。
- 未选择模型时可以回退同表默认模型，但必须在 trace 中标明来源。

### P2：字段、维度、字典闭环（基础开发）

目标：业务字典、字段绑定、维度绑定真正影响 SQL 字段选择。

开发内容：

- 解析用户问题中的业务词、同义词、维度词。
- 使用 `dictionaryEntries` 和 `dimensionSystem` 生成字段约束。
- 让 Java fallback 支持业务模型字段优先。
- 生成 SQL 后检查字段是否符合业务模型命中结果。

验收用例：

- 配置“收入 -> sales_amt”，输入“看收入趋势”，SQL 使用 `sales_amt`。
- 配置“城市维度 -> city”，输入“按城市看收入”，SQL 使用 `city`。
- 配置“营收、流水、GMV 都是收入”，输入“看 GMV 排名”，SQL 使用 `sales_amt`。
- 配置“省份维度 -> province”，输入“各省收入排名”，SQL 使用 `province`。

### P3：指标公式闭环（基础开发）

目标：业务公式不再只是提示，而是真正参与 SQL 表达式生成。

开发内容：

- 实现公式解析器。
- 实现字段白名单校验。
- 实现公式 SQL 编译。
- 支持常见公式聚合口径：先聚合后计算、逐行计算后聚合。
- 公式字段缺失、歧义或非法时提示确认。

验收用例：

- 配置“毛利率 = profit / sales_amt”，输入“各省毛利率排名”，SQL 使用 `profit` 和 `sales_amt` 组合表达式。
- 配置“客单价 = sales_amt / order_count”，输入“看每个月客单价走势”，SQL 使用公式表达式并按月聚合。
- 删除或改错公式字段后，查询应提示公式字段不可用，而不是生成错误 SQL。

### P4：SQL 后置校验与自动修正（基础开发）

目标：即使 AI 生成了错误字段，后端也能根据业务模型校验和修正。

开发内容：

- 新增业务模型一致性校验器。
- 对 AI SQL、缓存 SQL、Java fallback SQL 统一校验。
- 支持字段替换、维度替换、公式重建。
- 无法修正时进入确认或拒绝执行。

验收用例：

- 模型配置“收入 = sales_amt”，AI 返回 `amount` 时，后端自动修正为 `sales_amt`。
- 用户只问“收入”，不得错误使用“销售额、省份”等未提及业务项。
- 用户没有提维度，不得强行生成维度查询。
- “含税金额”对应多个字段时，提示确认，不直接写入或查询。

### P5：预测、预警、复合任务接入（进阶开发）

目标：预测、预警、多步骤编排复用同一套业务语义上下文。

开发内容：

- 预测字段解析优先使用业务模型指标。
- 预警规则指标字段优先使用业务模型指标。
- 复合任务中的查询、预测、预警、钉入看板共享同一业务语义解析结果。
- 对每个步骤输出业务语义 trace。

验收用例：

- 输入“预测未来三个月收入”，预测使用模型中的收入字段。
- 输入“收入低于 80 万提醒我”，预警规则使用模型中的收入字段。
- 输入“查收入走势，预测下个月，如果低于 80 万提醒我”，查询、预测、预警三步都使用同一业务语义。

### P6：缓存、审计和可观测性完善（进阶开发）

目标：让业务模型生效过程可追踪、可审计、可定位。

开发内容：

- 缓存 key 加入业务模型 ID 和版本。
- 业务模型更新后清理相关语义缓存。
- SQL 审计记录业务模型命中信息。
- 前端展示业务语义命中摘要。
- 管理端历史记录中展示模型命中详情。

验收标准：

- 修改“收入绑定字段”后，同一句查询不会复用旧 SQL。
- SQL 审计中能看到命中的模型、字段、维度、公式。
- 查询结果或思考过程能说明本次是否使用业务模型配置。

## 五、必须遵守的开发原则

- 优化时不能丢失以前的功能。
- 优化时不能影响其他功能。
- 文档、代码、配置、测试数据和新增脚本统一采用 UTF-8 编码。
- 无业务模型场景必须保持原有链路可用。
- 业务模型只在用户语义命中时生效，不能无差别覆盖字段选择。
- 用户没有提到维度时，不得强行生成维度操作。
- 用户只提到“收入”时，不得修改或使用“销售额、省份”等无关业务项。
- 用户表达“口径、以后、统一用、按...算”时，优先进入指标口径或公式更新，而不是业务字典。
- 字段、公式、维度存在歧义时，应提示确认，不得直接写入或执行。
- 业务公式必须经过安全校验后才能编译为 SQL。
- 所有新增链路必须可追踪、可测试、可回退。

## 六、建议回归测试集

### 1. 字段绑定生效

```text
把收入绑定到 sales_amt
看一下收入走势
```

期望：

- SQL 使用 `sales_amt`。
- 返回 `businessSemanticTrace.resolvedMetricField = sales_amt`。

### 2. 同义词生效

```text
把 GMV、流水、营收 都作为收入的同义词
看一下 GMV 排名前十的城市
```

期望：

- 指标命中“收入”。
- SQL 使用收入绑定字段。
- TopN 数量按用户表达生成。

### 3. 维度绑定生效

```text
把省份维度绑定到 province
各省收入排名
```

期望：

- SQL 使用 `province` 作为维度。
- 不应误用城市、区域等字段。

### 4. 公式生效

```text
新增指标公式：毛利率 = profit / sales_amt
看一下各省毛利率排名
```

期望：

- SQL 使用 `profit` 和 `sales_amt` 组合表达式。
- 返回 `formulaApplied = true`。

### 5. 模型更新后缓存失效

```text
把收入绑定到 sales_amt
看收入趋势
把收入改为 tax_sales_amt
看收入趋势
```

期望：

- 第二次查询不得复用旧 SQL。
- SQL 应使用 `tax_sales_amt`。

### 6. 预测链路生效

```text
预测未来三个月收入
```

期望：

- 预测指标字段使用业务模型中的收入字段。
- 预测卡片 trace 显示命中业务模型。

### 7. 预警链路生效

```text
如果收入低于 80 万提醒我
```

期望：

- 预警草稿中的 `metricField` 使用收入绑定字段。
- 不应误触发预测。

### 8. 复合任务生效

```text
查收入走势，预测下个月，如果低于 80 万提醒我
```

期望：

- 查询、预测、预警三个步骤都使用同一业务模型指标。
- 刷新后卡片和业务语义 trace 保留。

## 七、最终验收标准

业务模型语义执行闭环完成后，应达到以下效果：

- 用户配置字段后，对话查询能稳定使用该字段。
- 用户配置维度后，对话查询能稳定使用该维度。
- 用户配置同义词后，对话语义能稳定命中对应业务项。
- 用户配置公式后，普通查询能生成真实公式 SQL。
- 预测、预警、复合任务能复用业务模型语义。
- 业务模型更新后，旧 SQL 缓存不会继续污染结果。
- 每次结果都能看到业务模型是否命中、命中了什么、最终 SQL 是否通过一致性校验。

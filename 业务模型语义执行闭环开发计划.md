# 业务模型语义执行闭环待开发计划

> 编码要求：本文档及后续相关代码、配置、测试文件统一采用 UTF-8 编码。
>
> 兼容性要求：后续优化不得丢失已有功能，不得影响普通查询、TopN、趋势、对比、预测、预警、警报、建模、看板钉入、复合任务等既有链路；所有改造必须支持无业务模型场景平滑回退。
>
> 文档维护要求：本文档只保存待开发项。功能完成并通过回归后，应从待开发清单中移除，仅保留必要的完成摘要和后续事项。

## 一、已完成能力摘要

基础开发 P0 至 P4 已完成，当前业务模型配置已经从“提示信息”升级为普通对话查询链路中的“语义约束”。已落地能力包括：

- 对话查询加载当前业务模型上下文，优先级为：当前选中模型 > 最近应用模型 > 最近创建模型 > 同表默认模型。
- 支持业务字典、同义词、指标定义、维度绑定命中，并在 SQL 生成后进行一致性校验。
- 支持业务指标公式编译为安全 SQL 表达式，常见除法公式自动增加除零保护。
- AI SQL、语义缓存 SQL、Java fallback SQL 统一经过业务模型后置校验，必要时自动重建 SQL。
- 语义缓存 key 已纳入业务模型 ID 和版本，避免模型更新后继续复用旧 SQL。
- 查询响应返回 `businessSemanticTrace`，用于验收业务模型是否命中、命中了什么、最终 SQL 是否通过校验。
- P5 预测、预警、复合任务接入已完成：预测和预警优先使用业务模型指标，复合任务共享同一业务语义解析结果，每个相关步骤输出业务语义 trace。
- 已补充 P2 回归测试，覆盖字段/维度/字典/公式闭环、模型版本缓存隔离、预测/预警/复合任务业务语义复用。

## 二、待开发项优先级

### P6：缓存、审计和可观测性完善（进阶开发，优先级中）

目标：让业务模型生效过程在生产环境中可追踪、可审计、可定位。

开发内容：

- 业务模型更新后主动清理相关语义缓存、预测缓存和分析缓存。
- SQL 审计记录业务模型命中信息，包括模型 ID、版本、指标、维度、公式、是否自动修正。
- 预测、预警和复合任务审计记录业务模型命中信息，包括模型 ID、指标字段、公式表达式、时间字段和步骤 ID。
- 前端查询结果、预测卡片、预警草稿和思考过程展示业务语义命中摘要。
- 管理端历史记录展示模型命中详情，便于排查“为什么用了这个字段”。
- 增加异常场景观测：模型字段缺失、公式非法、歧义字段、无权限模型、缓存失效等。

验收标准：

- 修改“收入绑定字段”后，同一句查询、预测或预警不会复用旧 SQL 或旧分析参数。
- SQL 审计中能看到命中的模型、字段、维度、公式和自动修正原因。
- 预测/预警/复合任务记录中能看到业务模型命中信息和步骤级 trace。
- 查询结果、预测卡片、预警草稿或思考过程能说明本次是否使用业务模型配置。
- 模型未命中时 trace 清晰标明 `enabled=false` 或无语义约束，且原有链路不受影响。

## 三、后续必须遵守的开发原则

- 优化时不能丢失以前的功能。
- 优化时不能影响其他功能。
- 文档、代码、配置、测试数据和新增脚本统一采用 UTF-8 编码。
- 无业务模型场景必须保持原有链路可用。
- 业务模型只在用户语义命中时生效，不能无差别覆盖字段选择。
- 用户没有提到维度时，不得强行生成维度操作。
- 用户只提到“收入”时，不得修改或使用“销售额、省份”等无关业务项。
- 用户表达“口径、以后、统一用、按...算”时，优先进入指标口径或公式更新，而不是业务字典。
- 字段、公式、维度存在歧义时，应提示确认，不得直接写入或执行。
- 业务公式必须经过安全校验后才能编译为 SQL 或分析表达式。
- 所有新增链路必须可追踪、可测试、可回退。

## 四、待开发回归测试集

### 1. 模型更新后缓存主动失效

```text
把收入绑定到 sales_amt
看收入趋势
把收入改为 tax_sales_amt
看收入趋势
预测未来三个月收入
如果收入低于 80 万提醒我
```

期望：第二次查询、预测或预警不得复用 `sales_amt` 的旧缓存。

### 2. SQL 审计可观测性

```text
各省毛利率排名
```

期望：审计记录包含模型 ID、毛利率公式、维度字段、最终 SQL 是否自动修正。

### 3. 预测/预警步骤审计

```text
查收入走势，预测下个月，如果低于 80 万提醒我
```

期望：查询、预测、预警三个步骤的审计或历史记录都能看到同一业务模型指标和步骤级 trace。

### 4. 异常场景观测

```text
预测未来三个月不存在指标
```

期望：不应静默回退到错误字段；应在 trace 或错误信息中说明业务模型未命中或字段不可用。

# 语音交互开发计划

## 一、目标

为现有 `ChatBI-GenUI` 增加完整的语音交互能力，让用户可以通过语音完成查询输入、结果播报与基础交互控制，减少键盘和鼠标依赖，实现“解放双手”的使用体验。

本能力主要包含：

1. 语音输入查询：支持语音转文字，驱动现有自然语言分析流程。
2. 语音播报结论：支持文字转语音，对查询结果、图表结论、告警信息进行播报。
3. 可调语音参数：支持语速、音量调节，适配不同使用场景。
4. 方言识别适配：支持普通话与常见方言/口音识别增强。
5. 多端可用：兼容桌面端与大屏/看板播放场景。

## 二、总体方案

基于现有前后端分离架构，在前端增加语音采集与播报控制层，在后端增加语音服务适配层，并与现有 `LLM` 查询、看板渲染、告警播报流程打通。

### 2.1 语音输入链路

`麦克风采集 -> 降噪/静音检测 -> 语音转文字(STT) -> 文本预处理 -> 现有自然语言查询链路`

### 2.2 语音输出链路

`查询结果/图表结论 -> 结论摘要生成 -> 文字转语音(TTS) -> 播放控制 -> 语速/音量配置`

### 2.3 技术落点

- 前端：Vue3 + Vite + Element Plus
- 后端：Spring Boot + Spring Security + MyBatis-Plus
- AI 层：现有大模型网关、SSE 流式输出、结果摘要生成
- 扩展服务：独立语音服务或第三方语音 API 适配层

## 三、功能拆分

### 3.1 语音输入查询

实现内容：

- 语音按钮：按住说话 / 点击开始录音 / 点击结束识别
- 实时转写：录音过程中可选展示部分识别文本
- 识别结果回填：将转写文本自动填入现有查询输入框
- 一键发送：识别完成后可直接触发查询
- 纠错编辑：允许用户在发送前手动修改识别结果

关键要求：

- 支持短句查询和复杂问题输入
- 支持中途打断与重录
- 支持录音失败、识别超时、网络异常提示

### 3.2 语音播报数据结论

实现内容：

- 对查询结果生成结构化摘要
- 对图表核心结论生成播报文案
- 对预警卡片、诊断报告支持自动朗读
- 支持开始、暂停、继续、停止播报

关键要求：

- 播报内容要短、准、可听懂，避免直接朗读冗长表格
- 支持“只播报结论”与“完整播报”两种模式
- 对数值、百分比、同比环比等表达进行口语化转换

### 3.3 语速与音量控制

实现内容：

- 语速调节：慢速、标准、快速，或数值化滑杆
- 音量调节：前端音量滑杆，支持静音
- 参数持久化：保存用户个性化语音偏好

建议默认值：

- 语速：标准
- 音量：70%
- 播报模式：仅结论

### 3.4 方言识别适配

实现内容：

- 支持普通话优先识别
- 对常见方言口音做识别增强
- 根据用户配置切换识别模型或识别参数

适配策略：

- 优先采用支持多语种/多口音的 STT 服务
- 对业务高频词、行业黑话、专有名词做热词优化
- 对识别结果做业务词典纠错

## 四、技术架构设计

### 4.1 前端模块

- `VoiceRecordButton`：录音控制
- `VoiceTranscriptPanel`：转写文本展示
- `VoiceSettingsDrawer`：语速、音量、识别语言配置
- `VoicePlaybackBar`：播报控制条
- `VoiceStatusToast`：录音、识别、播报状态提示

### 4.2 后端模块

- `VoiceController`：语音相关接口
- `SpeechToTextService`：语音转文字适配
- `TextToSpeechService`：文字转语音适配
- `VoicePreferenceService`：用户语音配置管理
- `VoiceSummaryService`：结果摘要生成

### 4.3 数据存储

建议新增或扩展以下配置数据：

- 用户语音偏好表
- 语音识别历史记录表
- 播报任务记录表
- 热词词库配置表

## 五、接口设计

### 5.1 STT 接口

- `POST /api/voice/stt/start`
- `POST /api/voice/stt/stop`
- `POST /api/voice/stt/recognize`

返回内容：

- 识别文本
- 置信度
- 是否命中方言适配
- 是否命中业务热词

### 5.2 TTS 接口

- `POST /api/voice/tts/speak`
- `POST /api/voice/tts/stop`
- `GET /api/voice/tts/status/{taskId}`

返回内容：

- 音频地址或音频流
- 播报任务状态
- 播报时长

### 5.3 配置接口

- `GET /api/voice/preferences`
- `PUT /api/voice/preferences`

支持字段：

- `speechRate`
- `volume`
- `languageMode`
- `voiceType`
- `autoSpeakConclusion`

## 六、实施阶段

### 第一阶段：基础语音能力

- 完成录音采集
- 接通 STT 语音识别
- 将识别文本回填到查询框
- 完成基础 TTS 播报

交付结果：

- 用户可用语音发起查询
- 用户可听到结果播报

### 第二阶段：交互增强

- 增加语速、音量调节
- 增加播报暂停/继续/停止
- 增加识别纠错与热词词库
- 增加结果摘要生成

交付结果：

- 形成可控、可配置的完整语音交互闭环

### 第三阶段：方言与场景优化

- 引入方言适配
- 优化行业黑话识别
- 在看板、大屏、告警场景中自动播报
- 完成设备兼容性与性能优化

交付结果：

- 满足业务高频使用场景
- 提升复杂口音场景下的识别成功率

## 七、非功能要求

- 响应速度：语音识别首帧可感知，播报启动延迟可控
- 稳定性：支持网络抖动、识别失败重试、播报失败降级
- 安全性：录音与音频数据按权限隔离，避免越权访问
- 可配置性：支持按用户、角色、场景配置语音参数
- 可维护性：语音引擎与业务逻辑解耦，方便替换供应商

## 八、验收标准

1. 可通过语音输入完成一次完整查询。
2. 查询结果可自动生成语音播报。
3. 可在前端调整语速与音量并即时生效。
4. 支持至少一种方言/口音增强策略。
5. 语音功能与现有看板、告警、查询流程稳定联动。
6. 失败场景有明确提示与回退方案。

## 九、风险与应对

- 识别准确率不足：通过业务热词、词典纠错、方言模型增强解决。
- 播报内容过长：通过摘要生成与分段播报解决。
- 浏览器兼容问题：优先适配主流 Chromium 内核浏览器。
- 第三方语音服务波动：设计本地降级和服务切换机制。

## 十、后续建议

- 将语音能力纳入统一用户偏好中心。
- 将语音播报扩展到预警通知、诊断报告和会议汇报场景。
- 后续可增加“语音指令控制看板”能力，如切换图表、翻页、暂停轮播。

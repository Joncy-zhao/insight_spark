# 管理员端 AI 图表推荐规则配置模块待开发项

> 文档编码要求：本文档统一使用 UTF-8 格式编码保存，后续编辑、提交和传输时不得转换为 GBK、ANSI 或其他编码。
>
> 开发边界要求：后续开发必须保持向后兼容，不得破坏现有对话查询、数据上传、看板、诊断报告、SQL 审计、权限中心等功能。数据库变更必须通过启动迁移脚本自动执行，并保持幂等，不得清空、删除或破坏历史数据。

## 已完成能力不再列入计划

- 管理员端菜单入口、规则配置页面、规则列表、规则新增/编辑/删除。
- 规则启用/禁用、审计日志、规则测试、ECharts 预览。
- 企业图表颜色、字体、布局和默认动态渲染偏好配置。
- 后端规则表、偏好表、审计表和启动自动迁移脚本。
- 默认规则初始化：时序、分组对比、占比、明细。
- 推荐规则服务的基础数据画像、规则优先级、兜底策略。
- 用户对话查询结果接入管理员推荐规则，响应中输出命中规则、推荐说明、`chartType`、`optionTemplate` 和语音摘要配置。

## 1. 用户端图表推荐结果展示增强

- 在对话查询结果区域展示命中的规则名称、规则编码、场景类型和推荐原因。
- 在图表元信息中补充“AI 推荐图表”说明，便于用户理解系统为何选择折线图、柱状图、饼图/环形图或表格。
- 当管理员禁用某类规则后，用户端需要能明确展示当前命中的是兜底规则或其他规则。
- 前端需保持现有 `chartType`、`fieldMapping`、`optionTemplate` 渲染兼容。

## 2. 看板快照规则追溯

- 看板组件保存时记录命中的规则编码、规则名称、场景类型和推荐配置版本。
- 历史看板优先使用保存时的快照配置，避免管理员后续调整规则导致旧看板展示漂移。
- 看板详情或管理端预览中展示图表来源：手动配置、AI 推荐规则、历史快照。

## 3. 规则版本与回滚

- 新增规则版本表或版本字段，记录每次规则编辑后的版本号。
- 支持管理员查看某条规则的历史版本。
- 支持将规则回滚到指定历史版本。
- 回滚操作需要写入审计日志。

## 4. 规则导入导出

- 支持导出当前规则、图表偏好和动态渲染配置为 JSON 文件。
- 支持导入规则配置，并做编码冲突、JSON 安全、字段完整性校验。
- 导入时支持“仅新增”“覆盖同编码规则”“预览差异后确认”三种模式。

## 5. 更结构化的规则编辑表单

- 将匹配条件 JSON 拆为可视化表单项：
  - 字段类型条件。
  - 字段名关键词。
  - 指标数量。
  - 维度数量。
  - 行数范围。
  - 业务意图关键词。
  - 行业场景标签。
- 保留高级 JSON 编辑，但默认使用结构化表单，降低管理员误配置风险。

## 6. 时序预测渲染深度集成

- 与高级分析预测服务对接真实预测数据。
- 当时序规则开启预测时，在用户端图表中追加预测曲线。
- 支持 95% 置信区间上下界展示。
- 支持预测区间、历史区间、异常点和预测说明的图例配置。

## 7. 语音播报适配落地

- 用户端语音播报读取 `voiceSummary` 配置。
- 支持按规则配置播报顺序：标题、核心指标、最大值、最小值、趋势、异常点。
- 支持不同图表类型的播报模板。
- 支持管理员开启/关闭某类图表的语音摘要。

## 8. 动态更新与交互参数落地

- 将管理员配置的刷新间隔、动画、tooltip、dataZoom、增量渲染参数应用到用户端图表。
- 支持动态图表在看板场景按配置刷新。
- 支持图表过多分类时自动开启滚动图例或 dataZoom。

## 9. 权限与安全增强

- 明确限制只有 ADMIN 可访问规则写接口。
- 对规则测试接口增加样例数据行数、字段数和 JSON 大小限制。
- 对 ECharts option 模板做更细粒度白名单校验。
- 禁止危险字段进入前端渲染配置，例如脚本、函数体、外链 URL。

## 10. 自动化测试补充

- 后端增加规则推荐服务单元测试：
  - 时序趋势识别。
  - 分组对比识别。
  - 占比分析识别。
  - 明细兜底识别。
  - 自定义规则优先级。
  - 禁用规则后不再命中。
- 后端增加接口测试：
  - 规则导入导出。
  - 规则版本回滚。
  - 用户对话查询响应包含推荐规则信息。
- 前端增加构建和关键页面人工验收清单。

## 11. 后续验收重点

- 管理员规则调整后，用户对话查询能按新规则推荐图表。
- 历史看板不因规则变化而展示异常。
- 预测曲线和置信区间能在真实时序预测结果中正确渲染。
- 语音播报能读取规则配置并生成摘要。
- 所有数据库脚本均可重复启动执行，不影响已有数据。

# 析数灵犀：AI驱动的对话式智能BI系统当前阶段总结报告

## 一、当前完成情况

当前项目已经从初始 Demo 迭代为具备普通用户端与管理员端主链路的 ChatBI 系统，围绕期末任务要求补齐了 P0/P1 核心功能，并完成 P2 菜单与演示口径对齐。系统已覆盖数据上传、多文件合并、零代码业务建模、自然语言分析、SQL 安全审计、数据权限、官方数据源、知识图谱/GraphRAG 和智能诊断报告。

```text
Excel/CSV 批量上传
→ 多文件合并 / 字段关联合并
→ 动态建表
→ 字段语义沉淀
→ 零代码业务建模
→ 企业模型库发布 / 套用
→ Python AI 生成 SQL
→ GraphRAG 多跳上下文检索
→ Java 后端 SQL 安全审计
→ 敏感字段脱敏
→ 查询执行
→ ECharts 图表渲染
→ 权限控制
→ 官方数据源与联邦关联管理
→ 智能诊断报告与根因链路导出
```

项目分为三个部分：

- `insight-spark-frontend`：Vue3 + Element Plus + ECharts 前端系统。
- `insight-spark-backend`：Spring Boot 后端业务服务。
- `insight-spark-ai-service`：Python FastAPI AI 分析服务。

## 二、已完成模块

### 1. 基础工程整理

已完成：

- 后端统一响应格式。
- 前端由单页 Demo 改为多模块系统操作台。
- 后端去除硬编码查询表名，改为用户选择数据表。
- 保留 Java 兜底 Text-to-SQL，Python AI 服务不可用时仍可演示。
- 支持 Spring Boot 后端、Vue 前端、Python AI 服务三端独立启动。

涉及文件：

- `insight-spark-backend/src/main/java/com/insightspark/common/ApiResponse.java`
- `insight-spark-backend/pom.xml`
- `insight-spark-frontend/src/App.vue`
- `insight-spark-ai-service/main.py`

### 2. 数据上传 Excel/CSV 与零代码建模

已完成：

- 支持 `.xlsx`、`.xls`、`.csv` 文件上传。
- 支持最多 5 个文件批量上传。
- 支持相同/兼容表头合并。
- 支持指定字段关联合并，覆盖跨文件关联、VLOOKUP 类场景。
- 自动解析表头和数据内容。
- 自动推断字段类型：
  - `TEXT`
  - `NUMBER`
  - `DATE`
- 动态创建业务数据表。
- 保存上传表元信息和字段元信息。
- 支持数据预览和字段语义展示。
- 支持数据预览分页，返回总行数、页码和每页条数。
- 支持数据表重命名、删除。
- 支持一句话零代码业务建模。
- 支持个人业务模型沉淀。
- 支持发布到企业模型库。
- 支持从企业模型库套用模型到当前数据表。

后端接口：

- `POST /api/data/upload`
- `POST /api/data/upload-batch`
- `GET /api/data/tables`
- `GET /api/data/tables/{tableName}/fields`
- `GET /api/data/tables/{tableName}/preview`
- `GET /api/data/tables/{tableName}/preview-page`
- `POST /api/data/tables/{tableName}/rename`
- `POST /api/data/tables/{tableName}/delete`
- `GET /api/data/business-models`
- `POST /api/data/business-models`
- `POST /api/data/business-models/{modelId}/publish`
- `POST /api/data/business-models/{modelId}/apply`

涉及文件：

- `DataUploadController.java`
- `DataUploadService.java`
- `DynamicDataListener.java`
- `DataUploadView.vue`
- `App.vue`

自动创建数据库表：

- `is_data_table`
- `is_data_field`
- `is_business_model`

### 3. 对话查询与图表生成

已完成：

- 用户选择数据表后进行自然语言提问。
- 后端读取字段结构。
- 查询前检索 GraphRAG 上下文。
- 优先调用 Python AI 服务生成 SQL。
- Python 服务不可用时使用 Java 兜底逻辑。
- SQL 执行前进入安全审计。
- 查询结果返回前进行敏感字段脱敏。
- 前端使用 ECharts 自动渲染柱状图、折线图、饼图。
- 页面展示：
  - 用户问题
  - AI 生成 SQL
  - 字段匹配
  - 图表结果
  - SQL 风险等级
  - GraphRAG 上下文

后端接口：

- `POST /api/chat/ask`

涉及文件：

- `ChatController.java`
- `ChatBiService.java`
- `PythonAiService.java`
- `SqlAuditService.java`
- `KnowledgeGraphService.java`
- `insight-spark-ai-service/main.py`

### 4. Python AI 服务

已完成 Python FastAPI 服务。

当前接口：

- `GET /health`
- `POST /ai/schema-index`
- `POST /ai/text-to-sql`
- `POST /ai/chart-recommend`
- `POST /ai/diagnose`

当前 AI 能力：

- 字段语义匹配。
- 简化 Text-to-SQL。
- 图表类型推荐。
- Z-Score 异常检测。
- 维度贡献分析。
- 趋势变化分析。
- 根因假设生成。
- 关联因素图表块生成。
- Markdown 诊断报告正文生成。

涉及文件：

- `insight-spark-ai-service/main.py`
- `insight-spark-ai-service/requirements.txt`
- `insight-spark-ai-service/README.md`

### 5. SQL 安全审计中心

已完成：

- AI 生成 SQL 后不直接执行，先进入安全检测。
- 仅允许 `SELECT` 查询。
- 拦截危险关键字：
  - `DROP`
  - `DELETE`
  - `UPDATE`
  - `INSERT`
  - `ALTER`
  - `TRUNCATE`
  - `CREATE`
  - `GRANT`
  - `REVOKE`
- 检测是否访问当前授权表。
- 检测是否存在多语句风险。
- 检测 `SELECT *` 与缺少 `LIMIT` 风险。
- 检测敏感字段访问。
- 查询结果敏感字段脱敏。
- 慢查询识别。
- 慢查询熔断风险标记。
- SQL 审计规则启停。
- SQL 审计规则阈值配置。
- B 端手工提交 SQL 审计。
- SQL 审计日志导出 CSV。
- 前端支持查看 SQL 日志、风险等级、执行状态、耗时、错误信息、命中规则、敏感字段。

后端接口：

- `GET /api/audit/sql-logs`
- `GET /api/audit/sql-logs/export`
- `GET /api/audit/rules`
- `POST /api/audit/rules/{ruleCode}/status`
- `POST /api/audit/rules/{ruleCode}/config`
- `POST /api/audit/submit`

涉及文件：

- `SqlAuditService.java`
- `SqlAuditController.java`
- `ChatBiService.java`
- `SqlAuditView.vue`
- `App.vue`

自动创建数据库表：

- `is_sql_audit_log`
- `is_sql_audit_rule`
- `is_sensitive_field_rule`

### 6. 数据权限中心

已完成演示版权限闭环。

当前用户模型：

- 普通用户：`demo-user`
- 管理员：`admin`

已完成：

- 当前用户权限概览。
- 本人上传数据表统计。
- 已授权数据表统计。
- 待审批申请统计。
- 可访问数据表列表。
- 权限申请。
- 我的申请进度。
- 管理员审批。
- 审批通过后写入授权表。
- 数据预览、字段查看、对话查询均接入权限校验。
- 官方数据源表必须绑定用户或角色权限后才进入普通用户可查询列表。
- 权限中心展示官方授权数据源。
- 权限中心展示敏感字段权限清单与脱敏访问方式。
- 普通用户可对官方库表发起权限申请，审批通过后写入官方数据源授权。

后端接口：

- `GET /api/permission/overview`
- `GET /api/permission/accessible-tables`
- `GET /api/permission/accessible-official-tables`
- `GET /api/permission/requestable-tables`
- `GET /api/permission/sensitive-fields`
- `GET /api/permission/my-requests`
- `POST /api/permission/requests`
- `GET /api/permission/admin/requests`
- `POST /api/permission/admin/requests/{requestId}/review`

涉及文件：

- `PermissionService.java`
- `PermissionController.java`
- `DataUploadService.java`
- `DatasourceService.java`
- `PermissionCenterView.vue`
- `App.vue`

自动创建数据库表：

- `is_data_permission`
- `is_permission_request`
- `is_official_datasource_permission`

### 7. 官方数据源管理

已完成管理员端官方 MySQL 数据源管理。

已完成：

- 新增官方 MySQL 数据源。
- 保存连接配置。
- 编辑数据源配置。
- 删除数据源。
- 配置连接池参数：
  - 最大连接数
  - 连接超时
- 测试连接。
- 解析 Schema。
- Schema 解析完成后自动同步知识图谱。
- 查看数据库表结构。
- 查看字段结构。
- 修改字段业务名。
- 标记敏感字段。
- 启用/禁用数据源。
- 用户/角色权限绑定。
- 权限回收。
- 配置官方表与上传表的联邦跨库关联关系。

后端接口：

- `GET /api/datasources`
- `POST /api/datasources`
- `POST /api/datasources/{datasourceId}`
- `POST /api/datasources/{datasourceId}/delete`
- `POST /api/datasources/{datasourceId}/test`
- `POST /api/datasources/{datasourceId}/sync-schema`
- `POST /api/datasources/{datasourceId}/status`
- `GET /api/datasources/{datasourceId}/schema/tables`
- `GET /api/datasources/{datasourceId}/schema/tables/{tableName}/fields`
- `POST /api/datasources/schema/fields/{fieldId}`
- `GET /api/datasources/{datasourceId}/permissions`
- `POST /api/datasources/{datasourceId}/permissions`
- `POST /api/datasources/permissions/{permissionId}/revoke`
- `GET /api/datasources/{datasourceId}/federal-relations`
- `POST /api/datasources/{datasourceId}/federal-relations`

涉及文件：

- `DatasourceService.java`
- `DatasourceController.java`
- `DatasourceManageView.vue`
- `App.vue`

自动创建数据库表：

- `is_official_datasource`
- `is_official_schema_table`
- `is_official_schema_field`
- `is_official_datasource_permission`
- `is_federal_relation`

### 8. 知识图谱与 GraphRAG

已完成：

- 将上传表、上传字段、官方数据源、官方 Schema、敏感标签、诊断报告沉淀为节点与关系。
- 支持图谱同步。
- 支持图谱概览统计。
- 支持关键词检索。
- 支持多跳 GraphRAG 检索。
- 对话查询携带 GraphRAG 上下文。
- 智能诊断报告携带 GraphRAG 根因链路。
- 支持 MySQL 轻量图谱兜底。
- 支持 Neo4j HTTP 同步配置，默认关闭，可按需开启。

Neo4j 配置位置：

```yaml
insight:
  neo4j:
    enabled: false
    http-url: http://localhost:7474/db/neo4j/tx/commit
    username: neo4j
    password: neo4j
```

后端接口：

- `POST /api/knowledge-graph/sync`
- `POST /api/knowledge-graph/rebuild`
- `GET /api/knowledge-graph/overview`
- `GET /api/knowledge-graph/graph`
- `GET /api/knowledge-graph/search`
- `GET /api/knowledge-graph/search-bundle`
- `GET /api/knowledge-graph/multi-hop`

涉及文件：

- `KnowledgeGraphService.java`
- `KnowledgeGraphController.java`
- `KnowledgeGraphView.vue`
- `App.vue`

自动创建数据库表：

- `is_kg_node`
- `is_kg_edge`

### 9. 智能诊断报告

已完成：

- 选择数据表。
- 选择指标字段。
- 选择维度字段。
- 选择时间字段。
- 后端抽取数据样本。
- 调用 Python AI 服务分析。
- 接入 GraphRAG 多跳上下文。
- 生成 GraphRAG 根因链路。
- 保存诊断报告。
- 前端展示诊断结果。
- 支持历史报告查看。
- 支持 Markdown 导出。
- 支持 Word 导出。
- 支持真实 `.docx` 文件导出。
- 支持真实 `application/pdf` 文件导出。

Python 诊断能力：

- 数值统计：
  - 有效记录数
  - 合计
  - 平均值
  - 最大值
  - 最小值
  - 标准差
- Z-Score 异常检测。
- 维度贡献分析。
- 时间趋势变化分析。
- 根因假设生成。
- 关联因素图表块生成。
- 建议动作生成。
- Markdown 报告正文生成。

后端接口：

- `POST /api/diagnosis/run`
- `GET /api/diagnosis/reports`
- `GET /api/diagnosis/reports/{reportId}`
- `GET /api/diagnosis/reports/{reportId}/export`

涉及文件：

- `DiagnosisService.java`
- `DiagnosisController.java`
- `PythonAiService.java`
- `KnowledgeGraphService.java`
- `insight-spark-ai-service/main.py`
- `DiagnosisReportView.vue`
- `App.vue`

自动创建数据库表：

- `is_diagnosis_report`

## 三、当前数据库表汇总

当前后端启动时会自动创建以下系统表：

| 表名 | 作用 |
| --- | --- |
| `is_data_table` | 用户上传数据表元信息 |
| `is_data_field` | 用户上传数据字段元信息 |
| `is_business_model` | 零代码业务模型与企业模型库 |
| `is_sql_audit_log` | SQL 安全审计日志 |
| `is_sql_audit_rule` | SQL 审计规则配置 |
| `is_sensitive_field_rule` | 敏感字段识别与脱敏规则 |
| `is_data_permission` | 数据访问授权 |
| `is_permission_request` | 权限申请审批记录 |
| `is_official_datasource` | 官方数据源配置 |
| `is_official_schema_table` | 官方数据源表结构 |
| `is_official_schema_field` | 官方数据源字段结构 |
| `is_official_datasource_permission` | 官方数据源用户/角色授权 |
| `is_federal_relation` | Excel 与官方库联邦关联配置 |
| `is_kg_node` | 知识图谱节点 |
| `is_kg_edge` | 知识图谱关系 |
| `is_diagnosis_report` | 智能诊断报告 |

此外，用户上传文件后会动态创建业务数据表：

```text
biz_data_时间戳随机尾号
```

## 四、当前运行方式

### 1. 启动 Python AI 服务

```powershell
cd D:\Javatest\insight-spark\insight-spark-ai-service
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 2. 启动后端

```powershell
cd D:\Javatest\insight-spark\insight-spark-backend
mvn clean spring-boot:run
```

### 3. 启动前端

```powershell
cd D:\Javatest\insight-spark\insight-spark-frontend
npm run dev -- --host 0.0.0.0
```

### 4. 访问系统

```text
http://localhost:5173
```

## 五、已验证命令

后端测试：

```powershell
cd D:\Javatest\insight-spark\insight-spark-backend
mvn -q test
```

前端构建：

```powershell
cd D:\Javatest\insight-spark\insight-spark-frontend
npm run build
```

Python 语法检查：

```powershell
cd D:\Javatest\insight-spark\insight-spark-ai-service
python -m py_compile main.py
```

最近一次验证结果：

- 后端 `mvn -q test`：通过。
- 前端 `npm run build`：通过。
- Python `python -m py_compile main.py`：通过。

## 六、当前项目亮点

### 1. 数据接入不再是单文件 Demo

当前已支持批量上传、按表头合并、按指定字段关联、动态建表、数据表管理和零代码业务建模，覆盖图片任务中“多文件组合并配置、VLOOKUP/关联、零代码业务建模”的核心要求。

### 2. 模型可以沉淀和复用

业务模型会保存到 `is_business_model`，支持个人模型和企业模型库，用户可发布、套用和复用模型。

### 3. SQL 审计具备规则配置和导出

系统不只是记录 SQL，还支持审计规则开关、阈值配置、手工提交审计、敏感字段识别、结果脱敏、慢查询标记和 CSV 导出。

### 4. 官方数据源具备管理员管控能力

官方数据源支持连接测试、Schema 解析、字段中文名、敏感标记、连接池参数、用户/角色授权、联邦关联配置和图谱同步。

### 5. GraphRAG 链路已经接入

知识图谱支持 MySQL 轻量图谱兜底，也支持 Neo4j HTTP 同步配置。对话查询和诊断报告都会携带多跳检索上下文。

### 6. 智能诊断不只是统计摘要

Python AI 服务已经生成异常点、维度贡献、趋势变化、根因假设、关联因素图表块和建议动作；Java 后端进一步补充 GraphRAG 根因链路。

### 7. 菜单与期末任务口径对齐

前端菜单已按任务图片中的模块进行命名和分组：

- 普通用户端：
  - `2 数据上传（Excel/CSV）`
  - `3 对话查询（自然语言生成图表）`
  - `6 数据权限中心`
- 管理员端：
  - `5 官方数据源管理`
  - `6 数据权限审批`
  - `7 智能诊断报告`
  - `7 知识图谱与GraphRAG支撑`
  - `6 SQL安全审计中心`

其中“知识图谱与GraphRAG支撑”作为智能诊断报告和对话分析的支撑模块展示，便于期末验收时说明 GraphRAG 多跳检索能力。

## 七、当前仍可继续增强的内容

当前 P0、P1 和 P2 菜单完整度已补齐到可演示状态，后续如果追求更强工程化，可继续增强：

### 1. 接入真实大模型 API

当前 Text-to-SQL 仍以规则和字段匹配为主，可继续接入 DeepSeek、OpenAI、通义千问或本地大模型，提高复杂自然语言 SQL 生成准确率。

### 2. Neo4j 私有化部署联调

当前已提供 Neo4j HTTP 同步配置，默认关闭。若演示环境安装 Neo4j，可将 `insight.neo4j.enabled` 改为 `true` 后联调真实图数据库。

### 3. 更严格的 RBAC 与登录体系

当前用户仍是演示账号 `demo-user` 和 `admin`，但权限中心已经展示官方库授权、敏感字段权限和官方库申请审批链路。后续可继续接入登录、用户管理、角色管理和更完整的字段级授权策略。

### 4. 报告模板美化

当前已支持真正 `.docx` 和 `application/pdf` 文件导出。后续可继续补充封面、目录、图表图片嵌入、页眉页脚和企业模板样式。

### 5. 测试覆盖增强

当前保留原有 Spring Boot 启动测试，并通过手工构建验证三端可运行。按当前任务要求，本轮 P2 不新增测试文件；后续如需要工程化交付，可再补上传、权限、审计、数据源、诊断等接口级测试。

## 八、推荐演示顺序

建议期末演示按以下顺序：

1. 展示系统整体架构：Vue + Spring Boot + Python AI。
2. 在“数据上传”上传多个 Excel/CSV 文件。
3. 展示相同表头合并或指定字段关联/VLOOKUP 合并。
4. 输入一句话业务建模需求，展示生成的业务模型。
5. 发布模型到企业模型库，再套用到当前数据表。
6. 展示自动建表、分页数据预览、字段语义。
7. 进入“对话式分析”，输入自然语言问题。
8. 展示 AI 生成 SQL、GraphRAG 上下文、SQL 风险等级和 ECharts 图表。
9. 打开 SQL 安全审计中心，展示规则配置、手工 SQL 审计、日志筛选和导出。
10. 打开数据权限中心，展示权限范围、官方库授权、敏感字段权限、申请和审批。
11. 打开官方数据源管理，展示连接测试、Schema 解析、权限绑定和联邦关联。
12. 打开知识图谱与 GraphRAG，执行图谱同步和多跳检索。
13. 打开智能诊断报告，生成异常诊断报告，展示根因假设、关联因素图表块和 GraphRAG 根因链路。
14. 导出诊断报告 Markdown / DOCX / PDF。

## 九、当前总结

当前项目已经完成期末任务图片中 P0、P1 与 P2 菜单完整度要求：普通用户端的数据上传、批量合并、零代码建模、分页预览、权限中心，以及管理员端的官方数据源管理、智能诊断报告、SQL 安全审计中心和知识图谱/GraphRAG 增强。

系统不再只是前端演示页面，而是具备真实接口、数据库落表、动态建表、分页预览、权限校验、官方库授权、SQL 审计、敏感脱敏、模型沉淀、图谱检索、AI 诊断和 DOCX/PDF 报告导出的完整项目原型。后续重点可以放在真实大模型接入、Neo4j 环境联调、完整登录/RBAC 和报告模板美化上。

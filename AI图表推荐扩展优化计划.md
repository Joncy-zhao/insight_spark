# AI 图表推荐扩展优化计划

## 目标

在现有 AI 图表推荐仅覆盖折线图、饼图、柱状图的基础上，扩展支持雷达图、散点图、指标卡、地图，并确保对话查询结果、图表快照、钉入看板、看板编辑与看板预览中的图表类型保持一致。

## 重要约束

- 本次优化必须兼容现有功能，不得影响已上线的折线图、饼图、柱状图、表格、预测、预警、诊断报告、看板协作、看板分组、图表钉入、历史查询等功能。
- 新增图表类型必须走增量扩展，不修改现有接口字段语义，不删除现有字段，不改变历史数据读取逻辑。
- 后端、AI 服务、前端和数据库中的图表类型枚举必须统一，避免出现 AI 推荐成功但前端无法渲染、看板无法回显、规则配置无法保存的问题。
- 文档与后续代码文件均保持 UTF-8 编码。涉及 SQL 的文件继续使用 `utf8mb4` 字符集约定。

## 当前状态梳理

- AI 服务 `insight-spark-ai-service/main.py` 中 `/ai/chart-recommend` 当前只返回 `line`、`pie`、`bar`。
- AI 服务 Text-to-SQL 提示词中 `chartType` 约束仍为 `bar|line|pie`。
- AI 服务兜底函数 `choose_chart_type` 只基于时间字段和占比语义在 `line`、`pie`、`bar` 间选择。
- 图谱引导 SQL 结果中存在 `{"bar", "line", "pie"}` 白名单，需要同步扩展。
- 前端通用快照工具 `insight-spark-frontend/src/utils/chartOptionFromSnapshot.js` 已出现 `radar`、`scatter`、`map`、`metric` 的部分归一化与样式逻辑，但主查询页、看板入口、标签文案、规则配置和部分兜底渲染还需要补齐。
- 后端 `AiChartRuleConfigService` 的规则配置可选图表类型仍以 `line`、`bar`、`pie`、`doughnut`、`table` 为主，需要允许新增类型。

## 统一图表类型

| 图表 | 统一值 | 推荐触发条件 | 数据要求 | 看板展示方式 |
| --- | --- | --- | --- | --- |
| 雷达图 | `radar` | 多指标对比、能力模型、评分维度、综合评价、多个维度的同一主体画像 | 至少 3 个维度指标，数值型指标可聚合 | ECharts radar，支持快照和看板样式覆盖 |
| 散点图 | `scatter` | 分布、相关性、离群点、两个数值指标关系 | 至少 2 个数值字段，允许第 3 个字段作为分组或气泡大小 | ECharts scatter，支持坐标轴、点大小、透明度 |
| 指标卡 | `metric` | 单指标、总量、均值、同比/环比摘要、KPI 展示 | 1 个核心数值，可选对比值、单位、趋势方向 | 非 ECharts 或轻量卡片组件，保持图表快照结构 |
| 地图 | `map` | 地域、省份、城市、区域分布、地域排行 | 1 个地理维度 + 1 个数值指标，地名需规范化 | ECharts map，需注册地图资源和降级策略 |

## 实施计划

### 1. 枚举与契约统一

- 新增统一常量清单：`bar`、`line`、`pie`、`radar`、`scatter`、`metric`、`map`、`table`。
- 梳理并更新 AI 服务、后端 Java 服务、前端工具函数、看板编辑器、历史查询筛选、规则配置页中的图表类型白名单。
- 保持历史 `doughnut`、`donut` 继续归一化为 `pie`，避免旧配置失效。
- 输出统一的 `chartType` 说明，禁止不同模块使用 `card`、`indicator`、`kpi` 等别名直接落库；这些别名只在输入归一化阶段转换为 `metric`。

### 2. AI 服务推荐逻辑扩展

- 扩展 `/ai/chart-recommend` 规则：
  - 命中“趋势、走势、按时间”优先 `line`。
  - 命中“占比、比例、构成、份额”优先 `pie`。
  - 命中“雷达、能力、评分、画像、多指标对比”且字段满足多指标条件时推荐 `radar`。
  - 命中“散点、分布、相关、关系、离群、异常点”且存在两个数值字段时推荐 `scatter`。
  - 命中“指标卡、KPI、总量、当前值、核心指标、单指标”且结果适合单值展示时推荐 `metric`。
  - 命中“地图、地域、省份、城市、区域分布”且存在地理维度时推荐 `map`。
  - 其他情况保持 `bar` 作为默认兜底。
- 更新 Text-to-SQL Prompt 中 `chartType` 允许值，从 `bar|line|pie` 扩展为 `bar|line|pie|radar|scatter|metric|map`。
- 更新 `choose_chart_type` 与图谱引导 SQL 白名单，保证 LLM 输出、规则兜底和图谱修正结果一致。
- 对 `metric` 和 `scatter` 补充字段映射结构，例如：
  - `metric`: `metricKey`、`metric`、`compareMetricKey`、`unit`、`trendKey`。
  - `scatter`: `xMetricKey`、`yMetricKey`、`groupKey`、`sizeMetricKey`。
  - `radar`: `dimensionKeys` 或 `metricKeys`。
  - `map`: `geoKey`、`metricKey`、`geoLevel`。

### 3. 后端接口与规则配置

- 更新 `AiChartRuleConfigService` 中 `RENDER_CHART_TYPE_KEYS` 与偏好配置 `chartTypes`，加入 `radar`、`scatter`、`metric`、`map`。
- 检查 `SmartChatService`、`AdminChatQueryService`、`ChatController`、`AdvancedAnalysisService` 中图表类型中文名转换逻辑，补齐雷达图、散点图、指标卡、地图。
- 图表快照继续使用现有 `chartType`、`chartSnapshot`、`optionTemplate` 字段，不新增强依赖字段。
- 若需要初始化推荐规则，新增 SQL 迁移只追加新规则，不改旧规则，不覆盖管理员已有配置。
- 所有新增规则保存、回滚、导入导出、审计日志要沿用现有版本快照机制。

### 4. 前端查询页渲染与交互

- 更新 `App.vue` 中图表类型文案、图表切换选项、AI 推荐标识、历史筛选选项。
- 复用 `chartOptionFromSnapshot.js` 的通用构建能力，补齐主查询页 `renderChart` 对 `radar`、`scatter`、`map` 的 option 构建。
- `metric` 不强制使用 ECharts，优先设计为指标卡视图；需要保留导出和截图时的快照信息。
- 对不满足渲染条件的新增类型提供降级：
  - `radar` 数据不足 3 个指标时降级 `bar`。
  - `scatter` 不足两个数值字段时降级 `bar` 或 `table`。
  - `metric` 多行多列时选择第一核心指标并展示说明。
  - `map` 无可识别地理字段或地图资源未加载时降级 `bar`。

### 5. 看板与图表快照同步

- 更新 `DashboardGridEditor.vue` 左侧已钉图表标签样式，补齐 `radar`、`scatter`、`metric`、`map` 的颜色和中文名。
- 更新 `UserDashboardView.vue` 与 `LegacyInlineChart.vue`，确保新增类型能在普通看板、共享看板、旧版内嵌卡片中回显。
- 对 `metric` 在看板中使用固定比例指标卡布局，避免被 ECharts 空实例占位。
- 对 `map` 增加地图资源加载与失败提示，失败时显示可读的降级图表，不影响整张看板渲染。
- 保存看板 `layout_json` 时继续保留现有结构，只新增可选样式字段，不破坏旧看板打开和编辑。

### 6. 地图专项处理

- 明确地图范围优先级：全国、省级、城市级。首期建议先支持中国省份级地图。
- 增加地名标准化映射，例如“广东”“广东省”“Guangdong”统一为 ECharts 地图资源可识别名称。
- 地图资源采用本地静态资源或前端依赖注册，避免运行时依赖外链。
- 地图数据无法匹配时记录 `unmatchedGeoNames`，前端展示降级提示，后端不抛出阻断错误。

### 7. 指标卡专项处理

- 定义指标卡快照结构：
  - `value`: 当前指标值。
  - `label`: 指标名称。
  - `unit`: 单位。
  - `compareValue`: 对比值。
  - `compareLabel`: 对比口径。
  - `trend`: `up`、`down`、`flat`。
- 支持从普通 SQL 聚合结果中提取第一行第一数值字段作为默认指标。
- 保持指标卡可钉入看板、可在历史记录恢复、可被诊断报告引用。

### 8. 测试与验收

- AI 服务测试：
  - 输入“按月份看销售趋势”应返回 `line`。
  - 输入“各渠道销售占比”应返回 `pie`。
  - 输入“不同门店服务、价格、复购评分雷达图”应返回 `radar`。
  - 输入“客单价和复购率的相关性散点图”应返回 `scatter`。
  - 输入“展示本月销售额指标卡”应返回 `metric`。
  - 输入“各省销售额地图”应返回 `map`。
- 后端测试：
  - 新增类型可保存为 AI 图表规则。
  - 图表规则导入导出、版本回滚、测试规则接口不报错。
  - 历史查询、批量图表快照、钉入看板接口返回新增类型。
- 前端测试：
  - 对话查询页能渲染或合理降级新增类型。
  - 手动切换图表类型后能重新渲染。
  - 历史记录恢复、刷新页面恢复、钉入看板后回显正常。
  - 看板编辑器新增类型拖拽、缩放、保存、另存为、共享预览正常。
- 回归测试：
  - 折线图、饼图、柱状图原有推荐、渲染、钉入、看板回显不变。
  - 预测、预警、诊断报告图表快照不受影响。
  - 旧历史记录和旧看板可以继续打开。

## 风险与规避

- 风险：AI 输出新类型但字段映射不足。规避：后端和前端都提供数据条件校验与降级图表。
- 风险：地图资源缺失导致空白。规避：地图本地注册，失败时降级柱状图并提示未匹配地名。
- 风险：指标卡不是标准 ECharts option，影响看板统一渲染。规避：在看板卡片组件中按 `metric` 分支渲染，不创建空 ECharts 实例。
- 风险：管理员已有图表规则被覆盖。规避：迁移只追加，不覆盖；导入逻辑继续走预览和确认。
- 风险：新增枚举遗漏导致部分页面显示原始英文。规避：建立统一类型文案函数或共享映射表，减少分散硬编码。

## 分阶段排期

### 第一阶段：契约与后端通路

- 统一图表类型枚举和中文文案。
- 扩展 AI 服务推荐接口、Prompt、兜底逻辑和图谱白名单。
- 扩展后端规则配置、导入导出和历史查询类型处理。

### 第二阶段：前端渲染与看板联动

- 补齐查询页图表类型切换与渲染分支。
- 补齐看板编辑器、普通看板、共享看板、旧版卡片渲染。
- 增加指标卡与地图降级体验。

### 第三阶段：规则配置与验收

- 添加默认推荐规则和测试样例。
- 完成 AI 服务、后端接口、前端渲染、看板回归测试。
- 整理验收记录，确认新增功能不影响其他功能。

## 验收标准

- AI 推荐可返回 `radar`、`scatter`、`metric`、`map`，且旧类型 `bar`、`line`、`pie` 行为保持稳定。
- 新增类型能完成“生成图表 -> 历史恢复 -> 钉入看板 -> 看板编辑 -> 看板预览”的完整链路。
- 新增类型数据不足或资源缺失时能降级，不出现白屏、空图、控制台阻断错误。
- 管理员 AI 图表规则支持新增类型的创建、编辑、测试、导入导出、回滚。
- 所有新增与修改文件使用 UTF-8 编码，SQL 继续保持 `utf8mb4`。

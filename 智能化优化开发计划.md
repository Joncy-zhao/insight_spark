# Insight Spark 智能化剩余开发计划

## 1. 文档说明

本文档只保留当前仍未完成或仍需继续增强的智能化能力。已经接入主链路、已具备基础展示、已纳入回归测试或已完成观测落库的内容，不再作为待开发功能列入本计划。

后续开发必须遵守：

- 不得丢失既有 Text-to-SQL、预测、预警草稿、业务模型、看板、上传、权限、审计、诊断报告等流程。
- 新增智能能力优先复用现有服务和页面能力，不重复实现业务逻辑。
- 涉及写入、发布、删除、停用、权限变更、预警落库、看板钉入、任务重试等副作用操作，必须做权限校验、语义校验和确认。
- 字段、模型、看板、规则、任务、用户、数据源等目标对象不明确时，必须澄清或生成候选草稿，不能静默猜测。
- 每新增或修复一个智能语义能力，必须补充对应语义回归用例。
- 所有智能路由、动作执行、兜底、失败和确认状态必须可观测、可审计、可回滚。

## 2. P0：标准 Action Plan 与确认闭环

当前 Action Plan 已具备基础结构，但更多智能动作尚未统一接入，确认、澄清、回滚和重试还没有形成完整闭环。

待开发：

- 将图表规则、上传处理、联邦查询、审计、诊断、任务、协作等动作接入统一 Action Plan。
- 字段、模型、看板、规则、权限目标不明确时，返回澄清问题或候选草稿。
- 高风险动作默认生成草稿，不直接落库或执行。
- 用户确认后，能够把草稿转为真实业务操作，并记录确认人、确认时间和执行结果。
- 用户拒绝、补参或修改草稿后，Action Plan 状态能同步更新。

验收：

- 字段不明确时不执行 SQL、预测、预警、权限或模型变更。
- 目标看板、模型、规则近似匹配不唯一时不自动漂移。
- 写入、发布、删除、停用、权限变更、规则落库等操作必须先确认。
- Action Plan 中每个动作都有状态、依赖、置信度、输入参数、执行器和失败原因。

## 3. P0：多步骤任务编排增强

基础查询、预测、预警草稿链路已可编排，后续需要覆盖更多复合任务。

待开发：

- 扩展更多动作组合，例如图表规则更新、字段修正、数据质量检查、联邦查询、诊断报告、任务查询和协作邀请。
- 支持多步骤中的人工确认、澄清补参、重试和回滚。
- 增强跨会话上下文、历史产物和当前图表驱动的复合任务编排。
- 多步骤中某一步失败时，后续依赖步骤要可追踪地跳过、重试或回滚。

验收：

- 新动作类型不得破坏已支持的查询、预测、预警草稿编排。
- 需要补参或确认时，不得静默执行后续高风险动作。
- 多步骤失败、重试和回滚状态能在前端和审计日志中查看。

## 4. P0：预警规则与警报事件智能化

预警规则与警报事件的后端智能路由已覆盖规则草稿、规则修改/停用/删除、事件查询、事件解释、事件确认和事件关闭。后续只保留仍未完成的联动能力。

待开发：

- 前端对话侧补齐规则修改、停用、删除、事件确认、事件关闭的确认按钮和确认回传。
- 规则或事件对象不唯一时，展示候选项并让用户选择，不能只依赖数字 ID。
- 分派警报事件给指定用户或团队。
- 推送通知、重试推送、升级通知等动作接入自然语言确认草稿。
- 预警事件解释可选写回事件记录，并展示解释更新时间和解释来源。

验收：

- “最近有哪些报警”在前端展示预警事件卡片，而不是普通 SQL 或图表。
- “这个报警为什么触发”在有上下文事件时展示原因解释；无上下文时提示选择事件。
- 规则停用、删除、修改和事件确认、关闭在前端必须先确认，再执行。
- 事件分派、推送通知和推送重试必须确认并写审计。

## 5. P1：图表规则与渲染偏好

图表推荐和渲染偏好需要从自然语言入口稳定接入。

待开发：

- 创建或修改图表推荐规则。
- 修改预测图、明细表、金额单位、置信区间等渲染配置。
- 规则变更前展示草稿和影响范围。
- 规则变更后只影响后续渲染，不改写历史查询结果。

示例：

```text
以后趋势类问题默认用折线图
预测图显示置信区间
明细查询都用表格
金额类图表默认显示万元
```

验收：

- 能识别图表规则变更，而不是误判为普通查询。
- 修改前展示规则草稿。
- 修改后不影响已有图表和历史查询结果。

## 6. P1：上传、字段语义和多文件处理

数据上传后，字段修正、质量检查和多文件合并仍需自然语言接入。

待开发：

- 字段语义修正。
- 数据质量检查。
- 多文件合并计划。
- 上传表套用业务分析模型。

示例：

```text
把订单日期识别成时间字段
检查字段含义有没有识别错
这两个文件按客户ID合并
这个表套用销售分析模型
```

验收：

- 字段修正必须校验字段存在。
- 多文件合并只生成合并计划，执行前必须确认。
- 字段语义修正不能改写原始上传数据。

## 7. P1：官方数据源与联邦查询

上传数据和官方数据源之间的关联分析仍需稳定接入。

待开发：

- 联邦查询。
- 官方数据源关联计划。
- 官方区域、客户、组织等数据补齐。
- 上传数据与系统库一致性检查。

示例：

```text
把我上传的销售表和官方客户表关联分析
用官方区域表补齐省市信息
查这个上传数据和系统订单库是否一致
```

验收：

- 能识别上传表与官方表之间的关联需求。
- 字段关联不明确时必须追问。
- 不允许编造不存在的官方表或字段。

## 8. P1：诊断报告、任务和审计查询

诊断、任务和审计已有页面或基础入口，但对话能力仍需继续打通到真实业务结果。

待开发：

- 生成经营诊断报告。
- 查询任务状态。
- 解释任务失败原因。
- 重试失败任务。
- 查询 SQL 审计记录。
- 解释 SQL 被拦截原因。
- 分析慢查询。

示例：

```text
帮我诊断销售额下降原因
生成一份经营分析报告
刚才上传处理到哪了
失败的文件为什么失败
重新跑一下上次失败的任务
最近有哪些危险查询
解释这条 SQL 为什么被拦截
```

验收：

- 诊断报告复用当前图表、预测或查询结果上下文。
- 任务状态能基于会话上下文定位最近任务。
- 任务重试必须确认。
- 审计查询只能读取授权范围内的数据。
- SQL 风险解释应关联审计记录和拦截规则。

## 9. P2：权限变更、资产共享和协作邀请

权限、共享和协作属于组织级副作用操作，必须在确认、审计、权限校验和回滚机制成熟后再正式开放自然语言执行。

待开发：

- 自然语言创建或修改权限策略。
- 开放看板、模型、报告给团队或指定用户。
- 邀请他人共同维护看板或模型。
- 发布模型到团队或企业空间。

暂缓期间处理方式：

- 可以支持解释和草稿，不直接执行。
- 可以引导用户到现有页面手动操作。
- 管理员权限校验、确认机制、审计日志完善后再进入正式执行。

验收：

- 权限变更必须校验管理员权限。
- 资产共享必须确认目标用户、权限范围和有效期。
- 所有组织级副作用操作必须可审计、可追踪、可回滚。

## 10. 持续验收要求

每次智能化开发完成后必须验证：

- 后端语义回归测试通过。
- 新增语义已补充到 `insight-spark-backend/src/test/resources/smart-chat-intent-cases.json` 或对应专项测试。
- 前端能正确展示动作计划、澄清、确认、失败和结果卡片。
- 高风险动作没有绕过确认直接执行。
- 管理端历史记录和审计信息能还原关键路由、动作、状态和失败原因。

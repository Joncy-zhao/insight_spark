# Insight Spark 智能化剩余优化开发计划

## 1. 文档说明

本文档仅保留项目当前仍需继续建设或增强的智能化能力。已经在项目中完成基础接入、并通过当前主链路可用的功能，不再作为“待开发功能”重复列入计划，避免后续排期和验收时把已实现能力误判为未开发。

后续优化仍必须遵守以下约束：

- 不得丢失以前已经实现的功能。
- 不得影响 Text-to-SQL、预测、预警、业务建模、看板、上传、权限、审计、诊断报告等既有流程。
- 新增智能化能力应优先复用现有服务，不重复造业务逻辑。
- 涉及写入、发布、删除、权限变更、预警落库、看板钉入等有副作用操作，必须做权限校验、语义校验和必要确认。
- 所有源码、配置、提示词、测试数据和文档统一使用 UTF-8 编码。
- 所有智能路由、语义解析、动作执行和兜底行为都应可观测、可测试、可回滚。

## 2. 当前不再列入开发计划的已实现能力

以下能力已具备基础实现或已在现有链路中接入，后续只作为回归保障对象，不再作为独立待开发功能列入排期：

- 普通对话进入智能路由，由后端统一判断查询、预测、预警、业务模型、看板等意图。
- Text-to-SQL 普通查询、趋势、排名、对比等基础查询能力。
- TopN 任意数量排名查询的语义识别与 SQL 保护。
- 明确预测语义触发预测卡片，并默认关闭普通查询后的自动预测。
- 预测优先基于原始数据源聚合，上一轮查询结果预测仅作为受控兜底。
- 预警类语句与预测语句的基础区分。
- 业务模型语义操作的基础拆分，包括字段绑定、维度绑定、指标公式、指标口径、业务字典等。
- 看板类语义操作的基础识别和钉入执行。
- 当前图表、历史图表钉入看板时的显式图表上下文传递。
- 未发布看板的智能钉入约束和目标漂移保护。
- SSE 思考过程在预测、业务模型、看板钉入等智能操作中的基础展示与刷新保留。

## 3. 剩余核心问题

### 3.1 多步骤任务编排仍需完善

当前智能路由已经能识别多类单步动作，但复合语句仍需要更强的动作拆解、依赖编排和结果聚合能力。

待优化示例：

```text
查最近半年销售额走势，预测下个月，如果低于100万就提醒我。
```

期望拆解为：

```text
QUERY_SQL -> FORECAST -> ALERT_RULE_CREATE
```

需要补齐：

- 多 action 的结构化输出。
- action 之间的 `dependsOn` 关系。
- 前一步失败时的后续动作中止和解释。
- 多步骤结果统一展示。
- 多步骤过程中的确认、澄清和回滚。

### 3.2 预警和警报事件链路需要深度智能化

当前预警语义已有基础识别，但预警规则和警报事件仍需进一步区分和落地。

待建设能力：

- 创建预警规则。
- 修改预警规则。
- 删除或停用预警规则。
- 查询已触发警报事件。
- 解释警报原因。
- 关闭、确认、分派警报事件。

必须满足：

- “如果低于 80 万提醒我”进入规则创建。
- “最近有哪些报警”进入事件查询。
- “这个报警为什么触发”进入事件解释。
- 删除、停用、推送通知等动作必须确认。

### 3.3 图表规则与渲染偏好需要智能化接入

基础查询和看板钉入已接入，但图表推荐规则、展示偏好和渲染配置仍需智能化。

待优化示例：

```text
以后趋势类问题默认用折线图
预测图显示置信区间
明细查询都用表格
金额类图表默认显示万元
```

对应能力：

```text
CHART_RULE_CREATE
CHART_RULE_UPDATE
CHART_RENDER_CONFIG_UPDATE
```

验收标准：

- 能识别用户是在修改图表规则，而不是普通查询。
- 修改前展示规则草稿。
- 修改后不影响已有图表和历史查询结果。

### 3.4 上传、字段语义和多文件处理需要智能化接入

数据上传流程已有基础能力，但自然语言驱动的字段修正、数据质量检查和多文件合并仍需完善。

待优化示例：

```text
把订单日期识别成时间字段
检查字段含义有没有识别错
这两个文件按客户ID合并
这个表套用销售分析模型
```

对应能力：

```text
FIELD_SEMANTIC_FIX
DATA_QUALITY_CHECK
DATA_MERGE_PLAN
BUSINESS_MODEL_AUTO_APPLY
```

必须满足：

- 字段修正需要校验字段存在。
- 多文件合并只生成合并计划，执行前需要确认。
- 不能因字段语义修正影响原始上传数据。

### 3.5 官方数据源与联邦查询需要智能化接入

联邦查询和官方数据源能力需要被自然语言稳定调用。

待优化示例：

```text
把我上传的销售表和官方客户表关联分析
用官方区域表补齐省市信息
查这个上传数据和系统订单库是否一致
```

对应能力：

```text
FEDERATED_QUERY
DATASOURCE_JOIN_PLAN
OFFICIAL_DATA_ENRICH
```

验收标准：

- 能识别上传表与官方表之间的关联需求。
- 字段关联不明确时必须追问。
- 不允许 LLM 编造不存在的官方表或字段。

### 3.6 权限、审计和安全治理需要智能化接入

权限和审计属于高风险模块，需要只做草稿和解释，不能直接静默执行。

待优化示例：

```text
让华东销售经理只能看华东数据
给运营组开放这个看板
为什么我看不到这个客户的数据
最近有哪些危险查询
解释这条 SQL 为什么被拦截
```

对应能力：

```text
PERMISSION_POLICY_CREATE
PERMISSION_POLICY_EXPLAIN
ACCESS_DIAGNOSIS
AUDIT_QUERY
SQL_RISK_EXPLAIN
SLOW_QUERY_ANALYSIS
```

必须满足：

- 权限变更必须校验管理员权限。
- 权限变更默认生成草稿，不能直接落库。
- 审计查询只能读取授权范围内的数据。
- SQL 风险解释应关联审计记录和拦截规则。

### 3.7 诊断报告、任务和协作能力需要智能化接入

诊断、任务和协作已经有部分页面能力，但对话入口仍需进一步接入。

待优化示例：

```text
帮我诊断销售额下降原因
生成一份经营分析报告
刚才上传处理到哪了
失败的文件为什么失败
重新跑一下上次失败的任务
把这个模型发布给团队
让张三一起维护这个看板
```

对应能力：

```text
BUSINESS_DIAGNOSIS
REPORT_GENERATE
TASK_STATUS_QUERY
TASK_FAILURE_EXPLAIN
TASK_RETRY
ASSET_SHARE
COLLABORATION_INVITE
```

必须满足：

- 任务重试需要确认。
- 资产共享和协作邀请需要权限校验。
- 诊断报告应复用当前图表、预测或查询结果上下文。

## 4. 统一动作计划仍需增强

现有智能路由需要继续向标准化 Action Plan 收敛，便于多步骤编排、回归测试和审计。

建议保留统一结构：

```json
{
  "primaryIntent": "MULTI_STEP",
  "confidence": 0.91,
  "needClarification": false,
  "requiresConfirmation": true,
  "actions": [
    {
      "id": "a1",
      "type": "QUERY_SQL",
      "slots": {
        "metric": "销售额",
        "timeRange": "最近半年",
        "groupBy": "月份"
      },
      "confidence": 0.92
    },
    {
      "id": "a2",
      "type": "FORECAST",
      "dependsOn": ["a1"],
      "slots": {
        "metric": "销售额",
        "horizon": "1m"
      },
      "confidence": 0.9
    }
  ],
  "missingSlots": [],
  "reasoning": "用户同时表达了趋势查询和预测需求。"
}
```

后端必须继续承担确定性校验：

- 表是否存在。
- 字段是否存在。
- 字段类型是否匹配。
- 当前用户是否有权限。
- 模型、看板、规则是否属于当前用户或组织。
- SQL 是否只读。
- 阈值、时间范围、渠道是否合理。
- 是否涉及删除、发布、推送、权限变更等高风险动作。

## 5. 澄清与确认策略

### 5.1 置信度策略

```text
confidence >= 0.75
  低风险动作可直接执行；高风险动作生成草稿并请求确认。

0.45 <= confidence < 0.75
  生成草稿或候选项，提示用户确认。

confidence < 0.45
  不执行，进入澄清。
```

### 5.2 必须澄清的情况

- 用户没有说明关键指标。
- 用户没有说明目标看板、目标模型或目标规则。
- 用户提到的字段在数据表中不存在。
- 用户提到的业务口径存在多个可能字段。
- 目标对象重名或近似匹配不唯一。
- 用户意图涉及副作用，但缺少必要确认。

### 5.3 必须确认的情况

- 预警规则落库。
- 删除、停用、发布、共享、权限变更。
- 多文件合并执行。
- 任务重试。
- 资产发布到团队或企业空间。
- 会影响全局图表规则、模型口径或权限范围的操作。

## 6. 语义评测集建设

仍需建立覆盖全模块的语义评测集，防止“修好一个语句，破坏另一个语句”。

建议新增：

```text
insight-spark-backend/src/test/resources/smart-chat-intent-cases.json
```

建议覆盖：

```json
[
  {
    "question": "查最近6个月趋势并预测下个月",
    "expectedPrimaryIntent": "MULTI_STEP",
    "expectedActions": ["QUERY_SQL", "FORECAST"]
  },
  {
    "question": "最近有哪些报警",
    "expectedPrimaryIntent": "ALERT_EVENT_QUERY"
  },
  {
    "question": "以后趋势类问题默认用折线图",
    "expectedPrimaryIntent": "CHART_RULE_UPDATE"
  },
  {
    "question": "这两个文件按客户ID合并",
    "expectedPrimaryIntent": "DATA_MERGE_PLAN",
    "requiresConfirmation": true
  },
  {
    "question": "让华东经理只能看华东数据",
    "expectedPrimaryIntent": "PERMISSION_POLICY_CREATE",
    "requiresConfirmation": true
  },
  {
    "question": "解释这条 SQL 为什么被拦截",
    "expectedPrimaryIntent": "SQL_RISK_EXPLAIN"
  },
  {
    "question": "刚才上传处理到哪了",
    "expectedPrimaryIntent": "TASK_STATUS_QUERY"
  }
]
```

评测指标：

```text
intentAccuracy
slotAccuracy
multiStepAccuracy
clarificationPrecision
unsafeActionBlockRate
fallbackRate
```

## 7. 线上观测与审计

每次智能路由建议记录：

```text
question
conversationId
primaryIntent
actions
confidence
fallbackUsed
missingSlots
requiresConfirmation
chosenExecutor
success
failureReason
userConfirmed
```

需要支持按以下维度排查：

- 哪些语句经常低置信度。
- 哪些意图经常误判。
- 哪些 action 经常缺参数。
- 哪些高风险操作被正确拦截。
- 哪些兜底规则被频繁触发。

## 8. 剩余分阶段实施计划

本节按照当前项目风险和收益重新排序。优先级分为：

```text
P0 必须优先完成：影响智能路由稳定性和后续所有能力扩展。
P1 第二批完成：有明确业务价值，但不阻塞当前主链路稳定。
P2 暂缓项：风险较高、依赖较多，建议等 P0/P1 稳定后再做。
```

### 第一阶段：语义评测集、基础观测与回归机制（P0 必须优先完成）

原因：

- 当前多次出现“修好一个语句，另一个语句又回归”的情况。
- 没有语义评测集，就无法稳定判断智能化优化是否真的通用。
- 观测日志能帮助定位问题来自 LLM、规则兜底、前端上下文还是后端执行器。

目标：

- 建立语义评测集。
- 建立智能路由基础日志。
- 每次智能化改动都能跑语义回归。
- 记录路由意图、置信度、兜底情况、执行器、失败原因和确认状态。

验收：

- 覆盖查询、预测、预警、业务模型、看板钉入、TopN、对比、趋势等已实现主链路。
- 新增修复必须补充对应测试用例。
- 能统计路由准确率、兜底率、澄清率和失败原因。

### 第二阶段：标准 Action Plan 与澄清确认机制（P0 必须优先完成）

原因：

- 这是多步骤编排、预警草稿、权限草稿、上传合并计划等能力的共同基础。
- 没有统一动作结构，就会继续出现各模块各自解释用户语义的问题。
- 没有确认机制，高风险动作不能安全接入自然语言。

目标：

- 统一多 action 输出结构。
- 支持 `primaryIntent`、`actions`、`slots`、`confidence`、`missingSlots`、`requiresConfirmation`。
- 字段、模型、看板、规则、权限等目标对象不明确时必须澄清。
- 写入、发布、删除、权限变更、规则落库等副作用动作必须确认。

验收：

- 字段不明确时不静默执行，必须提示确认或追问。
- 目标看板、模型、规则近似匹配不唯一时不自动漂移。
- 高风险动作默认生成草稿，不直接落库。

### 第三阶段：多步骤任务编排（P0 必须优先完成）

原因：

- 用户自然语言经常是复合任务，而不是单一步骤。
- “查趋势并预测”“预测后提醒我”是智能 BI 的核心体验。

目标：

- 支持 `dependsOn`。
- 支持 `QUERY_SQL -> FORECAST`。
- 支持 `QUERY_SQL -> FORECAST -> ALERT_RULE_CREATE_DRAFT`。
- 支持部分成功、失败中止和结果聚合。

验收：

- “查最近半年销售额走势，预测下个月”能拆为查询和预测。
- “查趋势、预测、低于阈值提醒我”能生成预警规则草稿。
- 前一步失败时，后续依赖动作不继续误执行，并能解释原因。

### 第四阶段：预警规则与警报事件深度接入（P0 必须优先完成）

原因：

- 预警和预测非常容易被语义混淆，是当前智能化链路的高频风险点。
- 需要明确区分“创建规则”和“查看已触发事件”。

目标：

- 完成规则创建、修改、删除、停用的智能草稿。
- 完成警报事件查询、解释、关闭、确认。
- 删除、停用、推送通知等动作必须确认。

验收：

- “如果销售额低于 80 万提醒我”进入预警规则草稿。
- “最近有哪些报警”进入警报事件查询。
- “这个报警为什么触发”进入警报解释。
- 高风险操作必须确认。

### 第五阶段：图表规则、上传处理、联邦查询接入（P1 第二批完成）

原因：

- 这些能力有明确业务价值，但不阻塞当前对话查询、预测、预警和看板主链路。
- 建议在 P0 能力稳定后再接入，避免扩大回归面。

目标：

- 接入图表规则配置。
- 接入字段语义修正和数据质量检查。
- 接入多文件合并计划。
- 接入官方数据源关联和联邦查询。

验收：

- “以后趋势默认折线图”进入图表规则配置。
- “把订单日期识别成时间字段”进入字段语义修正。
- “这两个文件按客户ID合并”生成合并计划，执行前必须确认。
- “把上传销售表和官方客户表关联分析”能进入联邦查询或关联计划。

### 第六阶段：诊断报告、任务状态和审计查询接入（P1 第二批完成）

原因：

- 诊断、任务和审计查询属于增强型智能助手能力。
- 读取类能力风险较低，可以在主链路稳定后逐步接入。

目标：

- 接入诊断报告生成。
- 接入任务状态查询和失败原因解释。
- 接入 SQL 审计查询和风险解释。

验收：

- “帮我诊断销售额下降原因”能生成诊断报告。
- “刚才上传处理到哪了”能基于会话上下文查询任务状态。
- “解释这条 SQL 为什么被拦截”能关联审计记录和拦截规则。

### 第七阶段：权限变更、资产共享和协作邀请（P2 暂缓项）

暂缓原因：

- 权限变更、资产共享、协作邀请都属于组织级副作用操作。
- 一旦误执行，影响范围大，必须依赖成熟的确认、审计、权限校验和回滚机制。
- 建议在 P0/P1 完成并稳定后再接入自然语言执行。

暂缓范围：

- 自然语言创建或修改权限策略。
- 自然语言开放看板、模型、报告给团队或指定用户。
- 自然语言邀请他人共同维护看板或模型。
- 自然语言发布模型到团队或企业空间。

暂缓期间处理方式：

- 可以支持解释和草稿，不直接执行。
- 可以引导用户到现有页面手动操作。
- 管理员权限校验、确认机制、审计日志完善后再进入正式开发。

后续验收：

- 权限变更必须校验管理员权限。
- 资产共享必须确认目标用户、权限范围和有效期。
- 所有组织级副作用操作必须可审计、可追踪、可回滚。

## 9. 最终目标

项目后续智能化的重点不再是重复实现已经接入的查询、预测、业务模型和看板钉入，而是把这些能力与尚未接入的预警事件、图表规则、上传处理、联邦查询、权限、审计、诊断、任务和协作统一编排起来。

最终效果是：用户在一个对话框里输入自然语言，系统能稳定识别意图、拆解动作、校验权限、必要时追问或确认，并调用现有业务服务完成复杂 BI 工作流。

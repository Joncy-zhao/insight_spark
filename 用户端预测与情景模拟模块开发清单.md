# 用户端预测与情景模拟模块剩余开发清单

> 开发要求：本模块所有新增/修改文件统一使用 UTF-8 编码；开发过程中保持功能边界清晰，避免影响团队其他成员已开发或正在开发的功能。

## 1. 已完成内容

以下内容已实现，不再作为待开发项：

- 已将预测与情景模拟能力融合进用户端对话查询模块。
- 已支持预测、What-if、预警以弹窗形式展示详情。
- 已新增前端高级分析 API 封装。
- 已新增后端高级分析接口。
- 已新增 AI 服务端高级分析自然语言解析入口。
- 已实现 LLM 优先解析、规则兜底的基础链路。
- 已增强 LLM 解析上下文，包含字段元信息、时间字段、数值字段和上一轮字段映射。
- 已支持高级分析接口鉴权并注入用户上下文。
- 已支持基于数据源字段进行真实时序预测。
- 已支持基于上一轮真实查询结果进行时序预测。
- 已支持 Prophet-like 与 Holt-Winters 两种不同预测分支。
- 已支持预测参数确认弹窗，可修正时间字段、指标字段、聚合粒度、预测点数和算法。
- 已支持 Holt-Winters alpha、beta、gamma、季节周期参数配置。
- 已支持预测点数自定义。
- 已支持基础 What-if 真实推演接口。
- 已支持预测/推演接口失败时给出明确状态，不把模拟结果伪装为真实计算。

## 2. LLM 接入边界

### 2.1 LLM 继续负责

- 意图识别：普通查询、时序预测、What-if、预警设置。
- 参数抽取：周期、算法、变量变化、阈值、通知渠道。
- 字段语义映射：把业务口径映射到真实字段。
- 上下文补全：结合当前数据源、上一轮查询和字段元信息补足参数。
- 歧义确认文案：字段或参数不确定时生成确认问题。
- 结果解释：把算法结果转成业务可读说明。
- 建议生成：基于真实算法结果生成策略建议。

### 2.2 LLM 不负责

- 真实预测数值计算。
- What-if 数值推演。
- Z-Score 异常检测。
- 阈值判断。
- 权限校验。
- 数据脱敏。
- 离线任务调度。
- 邮件/钉钉推送执行。
- 方案保存和规则状态写入。

## 3. 剩余待开发清单

### 3.1 参数确认与字段修正

- What-if 推演需要增加变量字段确认流程。
- 预警设置需要增加规则确认流程。
- 参数确认需要支持过滤条件配置。
- 确认后的字段映射需要保存到本次分析记录中，便于复算。

### 3.2 时序预测能力增强

- 将当前 Prophet-like 轻量实现升级为真正的 Python Prophet 算法服务，或明确在 UI 中标注为“Prophet-like”。
- 支持过滤条件，例如只预测某地区、某品类、某渠道。
- 支持缺失时间点补齐、重复时间点合并、异常点处理策略。
- 支持按日、周、月、季度、年聚合时自动生成正确的未来时间轴。
- 预测结果增加算法参数说明和数据质量评分。

### 3.3 What-if 推演增强

- 支持变量调整方式：百分比变化、绝对值变化、设为固定值。
- 支持变量约束范围，例如最小值、最大值、业务上限。
- 支持多场景批量推演，例如保守、中性、乐观三组方案。
- 支持敏感性分析，输出变量影响排序。
- 支持基于业务公式推演，例如利润 = 收入 - 成本。
- 支持 LLM 生成推演解释，但数值必须来自后端计算。
- 当前相关性估计方式需要升级为更稳定的拟合算法或公式引擎。

### 3.4 离线智能预警落地

- 新增预警规则持久化表。
- 新增预警规则保存接口。
- 新增预警规则编辑、启用、停用、删除接口。
- 预警规则需包含数据源、指标、时间字段、聚合粒度、过滤条件、判断条件、阈值、检测周期、通知渠道。
- 支持阈值预警。
- 支持 Z-Score 异常检测。
- 预警规则需绑定用户、组织、数据源权限。
- 新增离线批处理 Agent 或定时任务，按规则周期轮询数据。
- 预警触发后生成预警事件记录。
- 预警事件应包含异常值、阈值、历史对比、触发原因和图表快照。

### 3.5 预警推送

- 对接邮件真实推送。
- 对接钉钉真实推送。
- 支持通知渠道可用性校验。
- 支持推送失败记录、失败原因和重试。
- 支持点击预警卡片跳回对话查询页面，并恢复规则、数据源和指标上下文。
- 预警原因说明可由 LLM 生成，但异常判断必须来自后端规则结果。

### 3.6 方案保存与历史复算

- 新增预测方案、推演方案、预警规则的持久化存储。
- 保存内容需包含数据源、字段映射、过滤条件、算法类型、算法参数、LLM 解析结果、用户确认结果和计算结果版本。
- 支持历史记录列表、详情、删除、重命名。
- 支持基于历史方案重新计算。
- 支持不同预测版本或推演版本对比。
- 支持将真实预测图表钉入我的看板。
- 钉入看板后应保留图表快照和可复算参数。

### 3.7 结果解释与可解释性

- LLM 基于真实算法结果生成业务解释。
- 解释内容需明确区分“算法计算结果”和“AI 解释建议”。
- 预测结果需解释置信区间、趋势方向、数据质量和不确定性。
- What-if 结果需解释变量影响方向、敏感性排序和推荐方案依据。
- 预警结果需解释触发原因、异常幅度、历史对比和建议动作。
- 所有解释应支持复制和保存到历史记录。

## 4. 剩余接口清单

- 参数确认结果保存接口。
- Python Prophet 预测接口或 Prophet-like 标识接口。
- What-if 多场景推演接口。
- What-if 敏感性分析接口。
- 预警规则新增/编辑/删除/启停接口。
- 预警规则列表和详情接口。
- 离线 Agent 检测结果接口。
- 预警事件历史接口。
- 邮件/钉钉推送配置查询接口。
- 推送结果和重试接口。
- 方案保存、历史记录、版本对比接口。
- 钉入看板接口。
- LLM 结果解释接口。

## 5. 验收清单

- What-if 和预警可进入参数确认流程。
- 用户修正字段后可重新执行推演或预警保存。
- Prophet 与 Holt-Winters 结果不同，且 UI 明确展示算法来源。
- What-if 支持多变量、多场景、敏感性分析。
- 预警规则可真实保存、启停、删除。
- 离线 Agent 可按规则生成预警事件。
- 邮件/钉钉推送可记录成功、失败和重试状态。
- 历史方案可查看、复算、对比和钉入看板。
- AI 解释与算法结果边界清晰。
- 普通对话查询保持原有行为。
- 本模块新增功能不影响团队其他模块功能。
- 中文内容在页面、接口、导出图表和保存记录中无乱码。

## 6. 建议开发顺序

1. 完善真实 Prophet 算法接入或 Prophet-like 显式标识。
2. 增强 What-if 参数确认、多场景和敏感性分析。
3. 开发预警规则持久化和管理接口。
4. 开发离线 Agent 轮询和预警事件生成。
5. 对接邮件/钉钉真实推送和推送记录。
6. 开发方案保存、历史复算、版本对比和钉入看板。
7. 增加 LLM 结果解释能力。
8. 完成多数据源、多指标、多场景回归测试。

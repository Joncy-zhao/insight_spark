# 对话查询模块测试验收脚本

> 适用范围：用户端“对话查询（自然语言生成图表）”、对话会话/历史产物、智能意图编排、图表渲染、SQL 审计与权限保护、管理员“对话查询实验室”和“管理员对话历史”。
>
> 验收目标：确认自然语言查询能够稳定完成“提问 -> 语义理解 -> SQL/智能动作 -> 审计/权限 -> 执行 -> 图表/表格/高级分析产物 -> 历史与管理员追溯”的完整闭环。

## 1. 验收信息

| 项目 | 内容 |
| --- | --- |
| 系统名称 | 析数灵犀 Insight Spark |
| 模块名称 | 对话查询模块 |
| 测试类型 | 功能验收、接口验收、异常验收、兼容性验收、回归验收 |
| 前端地址 | `http://localhost:5173` |
| 后端地址 | `http://localhost:8080` |
| AI 服务地址 | `http://localhost:8000` |
| 普通用户账号 | `demo-user / user123456` |
| 管理员账号 | `admin / admin123456` |
| 建议测试数据 | `test-data/智能诊断报告测试数据.csv` |
| 通过标准 | P0/P1 用例全部通过；P2 用例无阻塞问题；失败项均记录原因、截图、请求/响应和日志 |

## 2. 验收前置准备

### 2.1 启动服务

1. 在仓库根目录执行一键启动脚本：

```powershell
cd D:\siyouyun\homework\syy
.\start-all.ps1
```

2. 如果需要手动启动，按以下顺序执行：

```powershell
cd D:\siyouyun\homework\syy\insight-spark-ai-service
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

```powershell
cd D:\siyouyun\homework\syy\insight-spark-backend
mvn spring-boot:run
```

```powershell
cd D:\siyouyun\homework\syy\insight-spark-frontend
npm install
npm run dev -- --host 0.0.0.0
```

3. 检查端口：

```powershell
netstat -ano | findstr :8000
netstat -ano | findstr :8080
netstat -ano | findstr :5173
```

### 2.2 基础冒烟检查

| 编号 | 操作 | 期望结果 |
| --- | --- | --- |
| PRE-01 | 打开 `http://localhost:5173` | 前端登录页正常显示，无白屏 |
| PRE-02 | 访问 `http://localhost:8000/health` | 返回 AI 服务健康状态 |
| PRE-03 | 使用管理员账号登录 | 登录成功，进入管理员菜单 |
| PRE-04 | 使用普通用户账号登录 | 登录成功，进入用户菜单 |
| PRE-05 | 打开浏览器开发者工具 Network | 后续请求均携带 `Authorization: Bearer <token>` |

### 2.3 准备测试数据

1. 使用普通用户登录。
2. 进入“数据上传（Excel/CSV）”。
3. 上传 `D:\siyouyun\homework\syy\test-data\智能诊断报告测试数据.csv`。
4. 上传成功后记录生成的数据表名，本文统一记为 `{tableName}`。
5. 确认“数据预览”有数据，“字段语义”识别出以下关键字段：

| 字段 | 预期语义 |
| --- | --- |
| 日期 | 时间维度 |
| 区域 | 分类维度 |
| 渠道 | 分类维度 |
| 产品线 | 分类维度 |
| 销售额 | 数值指标 |
| 订单量 | 数值指标 |
| 退货率 | 数值指标/比例 |
| 库存周转天数 | 数值指标 |

## 3. 用户端对话查询验收

### 3.1 页面入口与初始状态

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-01 | P0 | 普通用户登录后进入“对话查询（自然语言生成图表）” | 页面正常打开；左侧为对话区，右侧为“智能可视化呈现”；无前端报错 |
| U-02 | P0 | 查看数据源下拉框 | 能看到上传表或官方数据源；选择 `{tableName}` 后状态保持 |
| U-03 | P1 | 查看业务模型下拉框 | 可选择业务模型；无模型时页面给出可用状态，不阻塞普通查询 |
| U-04 | P1 | 点击“切换模型”按钮 | 展示可选模型；选择模型后后续请求携带模型信息并展示模型名称 |
| U-05 | P1 | 未输入问题直接发送 | 前端阻止发送或后端返回“问题不能为空”，页面不崩溃 |

### 3.2 基础自然语言出图

| 编号 | 优先级 | 测试问题 | 期望结果 |
| --- | --- | --- | --- |
| U-10 | P0 | `按区域统计销售额` | 返回 SQL；结果有数据；生成柱状图或其他合理图表；显示维度“区域”和指标“销售额” |
| U-11 | P0 | `按日期看销售额趋势` | 返回 SQL；生成折线图或趋势类图表；日期顺序合理；图表可见且不空白 |
| U-12 | P0 | `各渠道销售额占比` | 返回 SQL；生成饼图或占比类图表；百分比/数值展示合理 |
| U-13 | P1 | `列出销售额最高的前10条记录` | 返回表格或 TopN 图表；行数不超过合理限制；排序与问题一致 |
| U-14 | P1 | `对比华东和华北的销售额和退货率` | 能识别区域筛选；返回对比结果；字段映射和 SQL 条件合理 |
| U-15 | P1 | `华东线上智能家居最近销售情况` | 能识别多个筛选条件；结果范围符合“华东/线上/智能家居” |

验收检查点：

- 图表区域不出现空白、重叠、控制台异常。
- “生成的 SQL”可查看并复制。
- “SQL风险”“审计说明”“敏感字段”“命中规则/推荐规则”等元信息展示正常。
- 返回字段至少包含 `sql`、`data`、`chartType`、`fieldMapping`、`riskLevel`、`reasoningLogs` 或同等可追溯信息。
- 结果写入对话消息，并生成 `queryHistoryId`、`conversationId`、`assistantTurnId` 等会话关联信息。

### 3.3 图表交互与结果呈现

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-20 | P0 | 完成一次查询后切换“图表类型” | 图表重新渲染；无空白；不改变原始结果数据 |
| U-21 | P1 | 切换“排序” | 图表顺序发生变化；页面无抖动或异常 |
| U-22 | P1 | 点击“导出图片” | 成功下载图片；图片能打开，内容与当前图表一致 |
| U-23 | P1 | 点击图表放大/查看按钮 | 弹窗或大图正常显示；关闭后页面状态保持 |
| U-24 | P1 | 表格类结果查看 | 表头、行数据、滚动区域正常；长字段不遮挡 |
| U-25 | P1 | 查看 GraphRAG/语义依据区域 | 命中时展示业务模型、字段映射、图谱候选；未命中时展示明确提示 |

### 3.4 会话管理与连续追问

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-30 | P0 | 点击“新建对话” | 创建新会话；当前消息列表清空或进入新会话上下文 |
| U-31 | P0 | 在新会话中提问 `按区域统计销售额` | 正常生成结果；会话列表中出现该会话 |
| U-32 | P0 | 基于上一条消息追问 `只看华东` | 请求携带 `parentTurnId`；结果基于上一轮上下文收窄 |
| U-33 | P1 | 点击某条系统消息的“继续追问” | 页面出现追问上下文提示；发送后上下文被正确关联 |
| U-34 | P1 | 打开会话管理，搜索会话关键词 | 能按标题/摘要过滤；结果准确 |
| U-35 | P1 | 重命名会话 | 名称更新并持久化，刷新后仍保留 |
| U-36 | P1 | 归档会话，再恢复会话 | 会话状态切换正确；列表筛选结果正确 |
| U-37 | P1 | 删除测试会话 | 删除后列表不再显示；当前会话不再指向已删除会话 |
| U-38 | P1 | 刷新页面后重新打开历史会话 | 消息、图表产物、SQL、追问关系能恢复 |

### 3.5 流式输出与停止生成

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-40 | P0 | 输入 `按日期看销售额趋势` 并发送 | 消息区出现“思考中/思考过程”；最终返回结果 |
| U-41 | P1 | 在生成过程中点击“停止生成” | 当前请求取消；页面显示已停止；后端历史记录为取消/失败可追踪状态 |
| U-42 | P1 | 模拟 SSE 不可用后再次提问 | 前端可降级为普通解析或给出明确错误；不会卡死 |
| U-43 | P1 | 连续快速发送两次问题 | 不出现重复错乱；按钮 loading/disabled 状态合理 |

### 3.6 历史产物抽屉

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-50 | P0 | 点击“历史产物” | 打开历史抽屉；能看到当前用户历史记录 |
| U-51 | P0 | 使用关键词搜索刚才的问题 | 只显示匹配记录；总数/分页正确 |
| U-52 | P1 | 按数据源、图表类型、风险等级、执行状态筛选 | 筛选条件生效；组合筛选无异常 |
| U-53 | P1 | 使用日期范围和快捷日期 | 返回指定时间范围内记录 |
| U-54 | P1 | 切换时间正序/倒序 | 记录顺序变化正确 |
| U-55 | P0 | 点击历史记录查看详情 | 展示基本信息、字段映射、SQL、语义依据、推理步骤、快照状态 |
| U-56 | P0 | 点击“恢复图表” | 右侧图表恢复为历史快照；数据和图表类型一致 |
| U-57 | P1 | 点击“重播”推理步骤 | 推理步骤按顺序展开，可暂停/全部展开 |
| U-58 | P1 | 点击“继续追问” | 回到当前会话并带入历史产物上下文 |
| U-59 | P1 | 点击“复制SQL” | 剪贴板内容为该历史记录 SQL |
| U-60 | P1 | 删除一条历史记录 | 用户端列表移除该记录；管理员端可看到治理留痕或删除状态 |

### 3.7 钉入看板

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-70 | P1 | 完成一次图表查询后点击“钉入我的看板” | 弹窗展示可编辑、未发布的看板 |
| U-71 | P1 | 选择看板并确认钉入 | 提示成功；进入“我的看板”可看到该图表组件 |
| U-72 | P1 | 尝试钉入已发布或无权限看板 | 不允许钉入，并给出明确提示 |
| U-73 | P2 | 从历史产物详情钉入看板 | 使用历史快照创建看板组件；组件数据与历史一致 |

### 3.8 语音输入与朗读

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| U-80 | P2 | 打开语音设置 | 展示浏览器语音能力、语言选项和历史记录 |
| U-81 | P2 | 点击语音输入并说出“按区域统计销售额” | 输入框出现识别文本；可发送查询 |
| U-82 | P2 | 点击消息朗读按钮 | 浏览器开始朗读；可暂停/继续 |
| U-83 | P2 | 清空语音记录 | 语音历史清空；页面不报错 |

## 4. 业务模型专项验收

业务模型验收仍基于 `test-data/智能诊断报告测试数据.csv` 上传后的 `{tableName}`。建议使用可清理的测试模型名称：`销售经营验收模型_{日期}`，并在验收记录中保存 `{businessModelId}`。

### 4.1 业务模型准备与维护

| 编号 | 优先级 | 操作/测试指令 | 期望结果 |
| --- | --- | --- | --- |
| BM-01 | P0 | 打开“对话查询”，选择 `{tableName}`，查看“业务模型”下拉框 | 能展示当前用户可用模型；无模型时提示“当前数据源下暂无业务模型”，不影响普通查询 |
| BM-02 | P0 | 点击“业务字典/公式维护” | 打开业务字典与业务公式维护面板；能查看或维护字段绑定、指标口径、维度体系 |
| BM-03 | P0 | 在对话中输入 `请为当前销售数据源创建业务模型，模型名称为销售经营验收模型。业务字典：GMV对应销售额，销售区域对应区域，销售渠道对应渠道，订单日期对应日期。指标定义：GMV按销售额求和。维度定义：销售区域、销售渠道、订单日期。` | 调用业务模型智能体；创建成功；返回 `modelId/focusModelId/activeBusinessModelId`；业务模型下拉刷新并选中新模型 |
| BM-04 | P0 | 打开新建模型详情 | 模型名称、所属表 `{tableName}`、业务字典、指标定义和字段绑定正确；`GMV -> 销售额`、`销售区域 -> 区域`、`销售渠道 -> 渠道`、`订单日期 -> 日期` |
| BM-05 | P1 | 修改模型名称或说明后保存 | 保存成功；刷新页面后名称、说明仍保留 |
| BM-06 | P1 | 在模型中新增指标公式 `客单价 = 销售额 / 订单量` | 公式保存成功；不允许包含 SQL 语句、脚本或外链；错误公式有明确校验提示 |
| BM-07 | P1 | 输入 `把GMV绑定到销售额，把销售区域绑定到区域` | 命中业务模型修正意图；当前模型字段绑定被更新；消息展示更新项数量 |
| BM-08 | P1 | 输入 `发布这个业务模型到企业模型库` | 当前模型发布成功；企业模型库可见；模型状态或 `published=true` |
| BM-09 | P1 | 输入 `取消发布这个业务模型` | 发布状态取消；企业模型库不再展示该模型或状态变为未发布 |
| BM-10 | P1 | 删除验收模型 | 删除成功；列表不再展示；已使用该模型生成的历史记录仍可查看快照和语义痕迹 |

### 4.2 业务模型驱动查询

| 编号 | 优先级 | 测试问题 | 期望结果 |
| --- | --- | --- | --- |
| BM-20 | P0 | 选择 `销售经营验收模型` 后输入 `按销售区域统计GMV` | 能用业务别名理解问题；SQL 使用真实字段 `区域`、`销售额`；图表维度显示销售区域/区域，指标显示 GMV/销售额 |
| BM-21 | P0 | `按订单日期看GMV趋势` | 命中业务模型时间口径；生成趋势图；`fieldMapping.dimensionKey` 指向日期字段 |
| BM-22 | P1 | `各销售渠道GMV占比` | 命中渠道别名；生成占比图；SQL 分组字段为渠道 |
| BM-23 | P1 | `按销售区域统计客单价` | 使用业务公式或指标定义计算；结果不为空；公式口径在语义依据/字段映射中可追溯 |
| BM-24 | P1 | 同一问题先不选模型再选模型分别执行 | 选择模型后语义依据中出现业务模型命中信息；字段映射更符合模型别名 |
| BM-25 | P1 | 在历史产物中查看 BM-20 记录 | 历史详情保留业务模型 ID、模型名称、字段映射、语义证据和生成 SQL |
| BM-26 | P1 | 管理员历史中查看 BM-20 记录 | “推理与图谱/对话上下文”能追溯业务模型、语义 SQL 保护、字段绑定或模型重建痕迹 |
| BM-27 | P2 | 上传同结构新表后执行“自动套用业务模型” | 能匹配已发布或本人模型；字段重绑定成功；无法匹配时说明原因 |

业务模型查询验收检查点：

- 请求上下文携带 `activeBusinessModelId`、`lastCreatedBusinessModelId` 或 `lastAppliedBusinessModelId`。
- 返回结果的 `businessSemanticTrace`、`semanticEvidence`、`reasoningLogs` 或管理员详情中能看到业务模型来源。
- 业务模型别名、维度、指标公式影响 SQL 生成，但最终 SQL 仍必须通过 SQL 审计。
- 模型字段绑定错误时必须返回明确错误，例如“未绑定有效字段/无法唯一确认字段”，不能生成误导性图表。

### 4.3 业务模型权限与边界

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| BM-30 | P0 | 普通用户查看业务模型列表 | 只能看到本人模型和已发布企业模型 |
| BM-31 | P0 | 普通用户尝试修改他人未发布模型 | 返回无权限，模型内容不变 |
| BM-32 | P1 | 普通用户套用已发布企业模型到 `{tableName}` | 套用成功，生成本用户可操作的模型副本或表级绑定 |
| BM-33 | P1 | 普通用户尝试套用无权限模型 | 返回无权套用提示 |
| BM-34 | P1 | 删除模型后继续查看历史记录 | 历史记录不丢失；快照、SQL、字段映射仍可追溯 |

## 5. 智能意图与高级分析验收

以下用例建议在基础查询通过后执行。高级分析、预警和看板相关数据不足时，允许记录为“环境不满足”，但页面和接口必须返回明确提示，不能静默失败。

| 编号 | 优先级 | 测试问题 | 预期意图/结果 |
| --- | --- | --- | --- |
| S-01 | P0 | `查最近6个月销售额趋势并预测下个月` | 识别为多步骤或预测流程；返回趋势查询结果和预测产物；`responseType` 为 `MULTI_STEP` 或 `FORECAST` |
| S-02 | P0 | `预测下个月销售额` | 返回预测图表/卡片；展示历史值、预测值或置信区间；无需 SQL 时说明原因 |
| S-03 | P1 | `如果销售额低于80万提醒我` | 生成预警规则草稿；`requiresConfirmation=true`；不会直接创建规则 |
| S-04 | P1 | 在预警草稿中点击确认创建 | 规则保存成功；卡片状态变为“已创建/已保存”；可在高级分析记录或预警规则中查看 |
| S-05 | P1 | `查最近半年销售额走势，预测下个月，如果低于100万就提醒我` | 生成多步骤执行计划，包含 `QUERY_SQL`、`FORECAST`、`ALERT_RULE_CREATE_DRAFT` |
| S-06 | P1 | `最近有哪些报警` | 若有预警事件则展示事件表格；无事件时展示空状态，不回退成普通 SQL |
| S-07 | P1 | `这个报警为什么触发` | 无事件上下文时返回澄清提示；有事件上下文时展示触发原因 |
| S-08 | P1 | `确认报警12` | 有事件 ID 时更新状态；无权限或不存在时提示失败原因 |
| S-09 | P2 | `把刚才这个图钉到销售看板` | 有图表和目标看板时执行钉入；缺少上下文时返回澄清 |
| S-10 | P2 | `把订单日期识别成时间字段` | 返回字段语义修正澄清/草稿，不误执行危险 SQL |
| S-11 | P2 | `让华东销售经理只能看华东数据` | 返回权限策略澄清/草稿，不直接越权改权限 |
| S-12 | P2 | `最近有哪些危险查询` | 返回审计查询澄清或审计入口提示，不影响普通用户权限边界 |
| S-13 | P1 | 基于 S-04 创建成功的预警规则，记录 `{alertRuleId}`，输入 `停用预警规则{alertRuleId}` | 命中 `ALERT_RULE_DISABLE`；规则状态更新为 `DISABLED`；消息提示“预警规则已停用”；历史记录保留规则 ID 和治理动作 |
| S-14 | P1 | 输入 `启用预警规则{alertRuleId}` | 命中 `ALERT_RULE_ENABLE`；规则状态更新为 `ACTIVE`；消息提示“预警规则已启用”；预警规则列表刷新后状态一致 |
| S-15 | P1 | 输入 `把预警规则{alertRuleId}阈值改成100万` | 命中 `ALERT_RULE_UPDATE`；规则阈值更新成功；消息展示更新后的规则摘要；无规则 ID 时应返回补充规则 ID 的澄清 |
| S-16 | P1 | 输入 `检测预警规则{alertRuleId}` | 命中 `ALERT_RULE_DETECT`；执行一次规则检测；返回检测数量、新增事件数量、跳过数量或当前规则不可检测原因 |

验收检查点：

- 智能路由结果中能追踪 `smartIntent`、`responseType`、`actionPlan`、`thinkingLogs`、`routeAudit` 等信息。
- 需要确认的副作用操作必须先给出草稿或澄清，不允许直接创建/删除高风险资产。
- 预警规则停用、启用、修改、检测必须携带明确规则 ID；未提供规则 ID 时返回澄清，不应误操作其它规则。
- 停用和启用属于规则治理动作，允许在识别到明确规则 ID 后通过对话直接执行；删除规则仍需确认后再执行。
- 预测、What-if、预警等非 SQL 产物在用户历史和管理员历史中也能正常回放。

## 6. 接口验收脚本

### 6.1 获取登录 Token

推荐先通过前端登录，再从浏览器控制台获取 Token：

```javascript
localStorage.getItem('token') || JSON.parse(localStorage.getItem('insight_auth') || '{}').token
```

后续 PowerShell 示例统一使用：

```powershell
$Base = "http://localhost:8080"
$Token = "<替换为登录后的 token>"
$Headers = @{ Authorization = "Bearer $Token" }
$Table = "<替换为上传后表名>"
```

### 6.2 用户端核心接口

| 编号 | 接口 | 方法 | 验收点 |
| --- | --- | --- | --- |
| API-U-01 | `/api/chat/models` | GET | 返回可用模型列表 |
| API-U-02 | `/api/chat/sessions` | POST | 创建会话，返回会话 ID |
| API-U-03 | `/api/chat/sessions` | GET | 支持分页、关键词、状态筛选 |
| API-U-04 | `/api/chat/sessions/{id}/messages` | POST | 会话内提问，返回图表/SQL/会话元信息 |
| API-U-05 | `/api/chat/sessions/{id}/messages/stream` | GET/POST | 返回 SSE 事件，最终包含结果或错误 |
| API-U-06 | `/api/chat/ask` | POST/GET | 兼容普通提问 |
| API-U-07 | `/api/chat/ask-smart` | POST | 智能意图路由 |
| API-U-08 | `/api/chat/history` | GET | 用户历史分页与筛选 |
| API-U-09 | `/api/chat/history/charts-batch` | POST | 批量获取图表快照 |
| API-U-10 | `/api/chat/business-model-agent` | POST | 对话式创建/修正/发布/套用业务模型 |
| API-U-11 | `/api/chat/business-model-agent-stream` | GET | 业务模型维护 SSE 过程输出 |

#### API-U-02 创建会话

```powershell
$Session = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/sessions" -Headers $Headers -ContentType "application/json" -Body (@{
  title = "验收测试会话"
  tableName = $Table
} | ConvertTo-Json)
$Session.data
```

期望：

- `code=200`。
- 返回 `id/title/status/createdAt/updatedAt` 等字段。

#### API-U-04 会话内提问

```powershell
$SessionId = $Session.data.id
$Ask = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/sessions/$SessionId/messages" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "按区域统计销售额"
  tableName = $Table
  enhanced = $true
} | ConvertTo-Json)
$Ask.data
```

期望：

- `code=200`。
- `data.sql` 非空，且只包含查询类 SQL。
- `data.data` 为数组且至少 1 行。
- `data.chartType` 合理。
- `data.fieldMapping.dimension` 和 `data.fieldMapping.metric` 合理。
- `data.riskLevel` 不为阻断级别，或阻断时有明确 `riskReason`。
- `data.conversationId` 等于 `$SessionId`。
- `data.queryHistoryId`、`data.userTurnId`、`data.assistantTurnId` 非空。

#### API-U-05 流式提问

```powershell
$StreamUrl = "$Base/api/chat/sessions/$SessionId/messages/stream?question=$([uri]::EscapeDataString('按日期看销售额趋势'))&tableName=$([uri]::EscapeDataString($Table))"
Invoke-WebRequest -Uri $StreamUrl -Headers $Headers -Method Get
```

期望：

- Content-Type 为 `text/event-stream`。
- 至少出现 `thinking`/`progress` 类事件和最终 `result` 事件。
- 错误时出现 `error` 事件，消息清晰。

#### API-U-07 智能意图

```powershell
$Smart = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/ask-smart" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "查最近6个月销售额趋势并预测下个月"
  tableName = $Table
  conversationId = $SessionId
} | ConvertTo-Json)
$Smart.data
```

期望：

- 返回 `smartIntent` 或 `responseType`。
- 多步骤场景返回 `actionPlan`、`thinkingLogs`。
- 预测/预警/澄清结果均可落历史。

#### API-U-07-A 预警规则停用/启用对话指令

前置：已通过预警草稿确认创建一条规则，并记录规则 ID 为 `$AlertRuleId`。

```powershell
$DisableRule = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/ask-smart" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "停用预警规则$AlertRuleId"
  tableName = $Table
  conversationId = $SessionId
} | ConvertTo-Json)
$DisableRule.data
```

期望：

- 返回 `smartIntent=ALERT_RULE_DISABLE` 或等价 `responseType`。
- 返回消息包含“预警规则已停用”或明确停用成功状态。
- 规则详情/规则列表中该规则状态为 `DISABLED`。

```powershell
$EnableRule = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/ask-smart" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "启用预警规则$AlertRuleId"
  tableName = $Table
  conversationId = $SessionId
} | ConvertTo-Json)
$EnableRule.data
```

期望：

- 返回 `smartIntent=ALERT_RULE_ENABLE` 或等价 `responseType`。
- 返回消息包含“预警规则已启用”或明确启用成功状态。
- 规则详情/规则列表中该规则状态恢复为 `ACTIVE`。
- 如果问题中没有明确规则 ID，应返回补充规则 ID 的澄清提示，不应操作任意规则。

#### API-U-08 用户历史

```powershell
$History = Invoke-RestMethod -Method Get -Uri "$Base/api/chat/history?page=1&pageSize=8&keyword=$([uri]::EscapeDataString('销售额'))&sortDirection=DESC" -Headers $Headers
$History.data
```

期望：

- 返回分页数据。
- 当前用户只能看到自己的历史。
- 列表项包含问题、表名、图表类型、风险、执行状态、快照状态、创建时间。

### 6.3 业务模型相关接口

| 编号 | 接口 | 方法 | 验收点 |
| --- | --- | --- | --- |
| API-BM-01 | `/api/data/business-models` | GET | 返回本人模型和可复用企业模型；`enterpriseOnly=true` 时仅返回已发布企业模型 |
| API-BM-02 | `/api/data/business-models/{modelId}` | GET | 返回模型详情、`modelJson`、发布状态、所属表 |
| API-BM-03 | `/api/data/business-models` | POST | 创建业务模型，支持 `dictionaryEntries` 和 `metricDefinitions` |
| API-BM-04 | `/api/data/business-models/{modelId}/update` | POST | 更新模型名称、说明、字典、指标、维度、分析逻辑 |
| API-BM-05 | `/api/data/business-models/{modelId}/publish` | POST | 发布/取消发布业务模型 |
| API-BM-06 | `/api/data/business-models/{modelId}/apply` | POST | 套用本人或企业模型到指定表 |
| API-BM-07 | `/api/data/business-models/{modelId}/delete` | POST | 删除本人可操作模型 |
| API-BM-08 | `/api/data/tables/{tableName}/auto-apply-model` | POST | 自动匹配并套用最合适业务模型 |
| API-BM-09 | `/api/chat/business-model-agent` | POST | 对话式业务模型智能体 |

#### API-BM-03 创建业务模型

```powershell
$BusinessModel = Invoke-RestMethod -Method Post -Uri "$Base/api/data/business-models" -Headers $Headers -ContentType "application/json" -Body (@{
  tableName = $Table
  modelName = "销售经营验收模型"
  requirement = "对销售数据建立经营分析业务模型"
  dictionaryEntries = @(
    @{ term = "GMV"; field = "销售额"; synonyms = "销售额,成交额,收入" },
    @{ term = "销售区域"; field = "区域"; synonyms = "区域,地区,大区" },
    @{ term = "销售渠道"; field = "渠道"; synonyms = "渠道,来源" },
    @{ term = "订单日期"; field = "日期"; synonyms = "日期,下单日期,时间" }
  )
  metricDefinitions = @(
    @{ name = "GMV"; field = "销售额"; aggregation = "SUM"; formula = "" },
    @{ name = "订单量"; field = "订单量"; aggregation = "SUM"; formula = "" },
    @{ name = "客单价"; field = "销售额"; aggregation = "AVG"; formula = "销售额 / 订单量" }
  )
} | ConvertTo-Json -Depth 6)
$BusinessModel.data
```

期望：

- 返回 `id/modelName/tableName/modelJson/published`。
- `published=false`。
- `modelJson` 中包含业务字典和指标定义。

#### API-BM-09 对话式创建或修正业务模型

```powershell
$ModelAgent = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/business-model-agent" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "把GMV绑定到销售额，把销售区域绑定到区域"
  tableName = $Table
  activeBusinessModelId = $BusinessModel.data.id
} | ConvertTo-Json)
$ModelAgent.data
```

期望：

- 返回 `handled=true` 或明确业务处理状态。
- 返回 `modelId/focusModelId/activeBusinessModelId`。
- 业务模型详情中字段绑定已更新。

#### API-BM 查询时携带业务模型上下文

```powershell
$AskWithModel = Invoke-RestMethod -Method Post -Uri "$Base/api/chat/sessions/$SessionId/messages" -Headers $Headers -ContentType "application/json" -Body (@{
  question = "按销售区域统计GMV"
  tableName = $Table
  activeBusinessModelId = $BusinessModel.data.id
  enhanced = $true
} | ConvertTo-Json)
$AskWithModel.data
```

期望：

- 结果 SQL 使用真实字段，语义展示使用业务别名。
- `semanticEvidence`、`businessSemanticTrace` 或 `reasoningLogs` 中能追踪业务模型命中。
- 历史记录和管理员详情保留模型上下文。

### 6.4 异常接口用例

| 编号 | 请求 | 期望结果 |
| --- | --- | --- |
| API-E-01 | `/api/chat/ask`，空 `question` | 返回业务错误“问题不能为空”或等价提示 |
| API-E-02 | 使用不存在的 `tableName` | 返回明确错误，不生成历史成功记录 |
| API-E-03 | 提问 `删除这张表` | SQL 审计阻断；不执行 DDL/DML |
| API-E-04 | 提问 `select * from is_user` | 系统表访问被阻断或脱敏；普通用户不能看到敏感数据 |
| API-E-05 | 无 Token 请求 `/api/chat/history` | 返回 401 或登录失效提示 |
| API-E-06 | 普通用户请求 `/api/admin/chat-history` | 返回无权限/401/403 |

## 7. 管理员对话查询实验室验收

### 7.1 页面基础能力

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AQL-01 | P0 | 管理员登录，进入“对话查询实验室” | 页面正常打开，展示配置区、SQL 与安全校验、图表渲染、执行结果、测试记录 |
| AQL-02 | P0 | 点击“刷新配置” | 数据源、模型、模板列表刷新成功 |
| AQL-03 | P0 | 选择测试数据源 `{tableName}` 和底层大模型 | 选择状态正常显示 |
| AQL-04 | P1 | 输入测试指令 `按区域统计销售额` | 输入框正常，按钮状态正确 |

### 7.2 流式测试与结果校验

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AQL-10 | P0 | 点击“流式测试” | 生成管理员测试会话；步骤区依次显示解析、数据源、NL2SQL、安全审计、查询执行、图表推荐、完成 |
| AQL-11 | P0 | 查看“生成的 SQL” | SQL 非空；可复制；与问题语义一致 |
| AQL-12 | P0 | 查看“SQL 与安全校验” | 风险等级、数据源类型、模型、会话状态展示正确 |
| AQL-13 | P0 | 查看“图表渲染测试” | 缩略图可见；图表类型可切换；“查看完整图表”正常 |
| AQL-14 | P0 | 查看“执行结果” | 表格有数据；“查看完整结果”弹窗正常 |
| AQL-15 | P1 | 点击测试记录“详情” | 可回显该次测试会话、步骤、产物 |
| AQL-16 | P1 | 点击测试记录“重试” | 重跑成功，生成新的执行结果或更新状态 |

### 7.3 模板、模型对比、权限穿透和导出

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AQL-20 | P1 | 保存当前测试指令为模板 | 模板保存成功；刷新后仍存在 |
| AQL-21 | P1 | 点击模板 | 指令被套用到输入框 |
| AQL-22 | P1 | 删除模板 | 模板从列表移除 |
| AQL-23 | P1 | 点击“模型对比”并开始对比 | 多模型串行执行；展示模型、风险、耗时、SQL |
| AQL-24 | P1 | 输入模拟普通用户 ID/角色，执行“权限穿透测试” | 展示管理员测试态与普通用户边界差异，结论明确 |
| AQL-25 | P1 | 点击“权限差异” | 弹窗展示差异维度、管理员范围、用户范围、结论 |
| AQL-26 | P1 | 点击“导出记录 Word” | 下载 `.docx`；文件可打开，包含问题、SQL、结果、审计信息 |
| AQL-27 | P1 | 点击“导出推理日志 Word” | 下载 `.docx`；文件包含推理步骤/链路日志 |

### 7.4 管理员实验室接口

| 编号 | 接口 | 方法 | 验收点 |
| --- | --- | --- | --- |
| API-AQL-01 | `/api/admin/chat-query/datasources` | GET | 返回可测数据源 |
| API-AQL-02 | `/api/admin/chat-query/models` | GET | 返回模型列表 |
| API-AQL-03 | `/api/admin/chat-query/templates` | GET/POST | 模板查询、保存成功 |
| API-AQL-04 | `/api/admin/chat-query/sessions` | POST/GET | 测试会话创建和分页 |
| API-AQL-05 | `/api/admin/chat-query/sessions/{id}/stream` | GET | SSE 步骤完整 |
| API-AQL-06 | `/api/admin/chat-query/sessions/{id}/permission-check` | POST | 权限差异结果完整 |
| API-AQL-07 | `/api/admin/chat-query/compare-models` | POST | 模型对比结果完整 |
| API-AQL-08 | `/api/admin/chat-query/sessions/{id}/export` | POST | 返回 Word 文件 |
| API-AQL-09 | `/api/admin/chat-query/sessions/{id}/reasoning-export` | POST | 返回推理日志 Word 文件 |

## 8. 管理员对话历史验收

### 8.1 列表、筛选和统计

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AH-01 | P0 | 管理员进入“管理员对话历史” | 页面正常打开；展示筛选区、历史记录、统计卡片和趋势图 |
| AH-02 | P0 | 搜索关键词 `销售额` | 能查到用户端刚生成的历史 |
| AH-03 | P0 | 按用户 ID/昵称筛选 `demo-user` | 只显示该用户相关记录 |
| AH-04 | P1 | 按表名/数据源筛选 `{tableName}` | 结果匹配该数据源 |
| AH-05 | P1 | 按模型、风险、状态、图表类型筛选 | 组合筛选生效 |
| AH-06 | P1 | 按缓存命中、慢查询筛选 | 返回匹配记录或明确空状态 |
| AH-07 | P1 | 切换时间正序/倒序 | 表格排序正确 |
| AH-08 | P1 | 查看统计卡片和趋势分析 | 总量、风险、缓存、慢查询趋势随筛选刷新 |

### 8.2 详情抽屉与追溯能力

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AH-10 | P0 | 点击历史记录“查看详情” | 打开详情抽屉 |
| AH-11 | P0 | 查看“查询与结果”页签 | 展示原始问句、生成 SQL、摘要、图表缩略图、结果预览 |
| AH-12 | P0 | 查看“权限与审计”页签 | 展示权限校验、缓存上下文、脱敏信息、SQL 审计日志 |
| AH-13 | P0 | 查看“推理与图谱”页签 | 展示模型推理过程、知识图谱上下文、完整图谱关系 |
| AH-14 | P0 | 查看“对话上下文”页签 | 展示会话轮次、用户/助手消息、图表/SQL/高级分析产物 |
| AH-15 | P1 | 复制 SQL | 剪贴板内容正确 |
| AH-16 | P1 | 点击“重新执行” | 后端重跑历史，返回新历史 ID 或新结果 |
| AH-17 | P1 | 查看高级分析历史详情 | 预测/What-if/预警类记录不强制展示 SQL，但展示算法产物和说明 |
| AH-18 | P1 | 打开完整图谱关系 | 图谱节点/关系完整展示，弹窗可关闭 |

### 8.3 删除、导出和治理审计

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| AH-20 | P1 | 单条删除历史记录 | 记录从列表移除；提示保留治理留痕天数 |
| AH-21 | P1 | 批量选择并删除 | 批量删除成功；审计数量正确 |
| AH-22 | P1 | 导出 Excel | 下载文件可打开；字段包含用户、问题、SQL、状态、风险、耗时等 |
| AH-23 | P1 | 删除后刷新列表和统计 | 列表与统计同步更新 |
| AH-24 | P2 | 普通用户尝试访问管理员历史 | 被拒绝或重定向，无越权数据泄露 |

### 8.4 管理员历史接口

| 编号 | 接口 | 方法 | 验收点 |
| --- | --- | --- | --- |
| API-AH-01 | `/api/admin/chat-history` | GET | 支持分页、关键词、用户、表名、来源、图表、风险、状态、模型、日期、缓存、慢查询、排序 |
| API-AH-02 | `/api/admin/chat-history/{historyId}` | GET | 返回详情、SQL、快照、审计、推理、图谱 |
| API-AH-03 | `/api/admin/chat-history/{historyId}/context` | GET | 返回对话上下文和产物 |
| API-AH-04 | `/api/admin/chat-history/analytics` | GET | 返回统计与趋势 |
| API-AH-05 | `/api/admin/chat-history/{historyId}/rerun` | POST | 重新执行并返回结果 |
| API-AH-06 | `/api/admin/chat-history/batch` | DELETE | 批量删除并写治理审计 |
| API-AH-07 | `/api/admin/chat-history/export` | GET | 导出 Excel |

## 9. SQL 安全、权限和异常验收

| 编号 | 优先级 | 场景 | 输入/操作 | 期望结果 |
| --- | --- | --- | --- | --- |
| SEC-01 | P0 | 非 SELECT 拦截 | `删除这张表`、`drop table is_user` | 审计阻断；无数据破坏；历史记录风险明确 |
| SEC-02 | P0 | 系统表拦截 | `查询 is_user 表所有用户` | 普通用户无法访问系统表；管理员也需显示风险 |
| SEC-03 | P0 | 敏感字段脱敏 | 查询包含手机号/邮箱/敏感字段的数据 | 返回结果脱敏或审计标记敏感字段 |
| SEC-04 | P1 | 权限边界 | 普通用户查询无权限官方数据源 | 返回无权限或权限申请提示 |
| SEC-05 | P1 | 行级权限 | 有行级策略时查询跨区域数据 | 结果只包含授权范围 |
| SEC-06 | P1 | 慢查询/超时 | 构造大范围聚合或复杂查询 | 超时/慢查询被记录；页面提示清晰 |
| SEC-07 | P1 | 语义缓存 | 重复执行同一问题 | 第二次可命中缓存或明确记录未命中原因 |
| SEC-08 | P1 | AI 服务不可用 | 停止 AI 服务后提问 | 使用规则兜底或返回友好错误；不白屏 |
| SEC-09 | P1 | 数据源不存在 | 使用已删除表继续提问 | 返回表不存在/数据源不可用提示 |
| SEC-10 | P2 | 并发查询 | 同一用户同时发起多条查询 | 超出限制时队列/熔断逻辑生效 |

## 10. 兼容性与刷新恢复

| 编号 | 优先级 | 操作 | 期望结果 |
| --- | --- | --- | --- |
| C-01 | P1 | Chrome/Edge 最新版分别执行基础查询 | 页面布局、图表、请求均正常 |
| C-02 | P1 | 1366x768、1920x1080、移动窄屏查看页面 | 控件不重叠；文本不溢出；图表区域可用 |
| C-03 | P1 | 查询完成后刷新页面 | 登录状态、当前会话、历史产物可恢复 |
| C-04 | P1 | 后端重启后重新登录 | 历史、会话、管理员记录仍可查询 |
| C-05 | P2 | 网络慢速环境执行流式查询 | loading、停止按钮、错误提示表现正常 |

## 11. 自动化回归建议

### 11.1 后端单元/集成测试

```powershell
cd D:\siyouyun\homework\syy\insight-spark-backend
mvn test
```

重点关注：

- `P2AcceptanceTests`
- `smart-chat-intent-cases.json`
- `ChatBiService`
- `SmartChatService`
- `BusinessModelAgentService`
- `BusinessSemanticService`
- `DataUploadService`
- `ChatQueryHistoryService`
- `AdminChatQueryService`

### 11.2 前端构建测试

```powershell
cd D:\siyouyun\homework\syy\insight-spark-frontend
npm run build
```

期望：

- 构建成功。
- 无未处理的 Vue 编译错误。
- 对话查询相关页面无资源引用错误。

### 11.3 Python AI 服务语法检查

```powershell
cd D:\siyouyun\homework\syy
python -m py_compile insight-spark-ai-service\main.py
```

期望：

- 无语法错误。

## 12. 验收记录模板

| 用例编号 | 执行人 | 执行时间 | 结果 | 问题描述 | 截图/日志/请求 ID | 处理结论 |
| --- | --- | --- | --- | --- | --- | --- |
| U-10 |  |  | 通过/失败/阻塞 |  |  |  |
| U-31 |  |  | 通过/失败/阻塞 |  |  |  |
| BM-03 |  |  | 通过/失败/阻塞 |  |  |  |
| BM-20 |  |  | 通过/失败/阻塞 |  |  |  |
| S-01 |  |  | 通过/失败/阻塞 |  |  |  |
| AQL-10 |  |  | 通过/失败/阻塞 |  |  |  |
| AH-10 |  |  | 通过/失败/阻塞 |  |  |  |
| SEC-01 |  |  | 通过/失败/阻塞 |  |  |  |

## 13. 验收通过准则

1. 用户端基础查询、会话追问、历史恢复、SQL 审计、图表渲染全部通过。
2. 业务模型创建、选择、字段绑定修正、业务别名查询、模型语义追溯至少各通过 1 条主路径。
3. 智能意图中预测、预警草稿、多步骤编排至少各通过 1 条主路径。
4. 管理员实验室能够完成一次流式测试、一次权限穿透、一次导出。
5. 管理员历史能够查询到用户端产生的记录，并完整查看 SQL、审计、推理、图谱、业务模型和对话上下文。
6. 危险 SQL、无权限访问、空问题、数据源不存在、AI 服务异常均有明确错误提示，不能造成白屏、卡死或数据破坏。
7. 后端 `mvn test`、前端 `npm run build`、AI 服务语法检查通过，或已记录可接受的环境原因。

## 14. 缺陷分级建议

| 等级 | 判定标准 | 示例 |
| --- | --- | --- |
| P0 | 主链路不可用、数据泄露、危险 SQL 执行、系统崩溃 | 普通查询无法返回；普通用户看到管理员历史；DROP 被执行 |
| P1 | 核心能力异常但有绕行方式 | 历史无法恢复图表；流式输出失败但普通查询可用；管理员详情缺审计信息 |
| P2 | 非核心能力异常或体验问题 | 语音不可用；导出文件格式轻微异常；图表排序交互不够清晰 |
| P3 | 文案、样式、兼容性小问题 | 标签文案不统一；边距异常；空状态提示不准确 |

## 15. 最小验收路径

如果时间有限，至少执行以下 15 个用例：

1. `PRE-01` 到 `PRE-05`
2. `U-10` 基础出图
3. `U-11` 趋势图
4. `U-32` 连续追问
5. `U-55` 历史详情
6. `U-56` 恢复图表
7. `BM-03` 对话式创建业务模型
8. `BM-07` 业务模型字段绑定修正
9. `BM-20` 业务模型别名查询
10. `S-01` 多步骤预测
11. `SEC-01` 危险 SQL 拦截
12. `AQL-10` 管理员实验室流式测试
13. `AQL-24` 权限穿透测试
14. `AH-10` 管理员历史详情
15. `AH-22` 管理员历史导出

执行完最小路径且全部通过后，可认为对话查询模块具备提测/演示条件；上线前仍需完成完整用例表。
